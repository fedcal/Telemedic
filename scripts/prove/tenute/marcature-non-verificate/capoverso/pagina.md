# Tenuta - il destinatario sta nella riga successiva dello stesso capoverso

Il termine di conservazione non e' stato letto sul testo vigente e resta [NV],
perche' la fonte primaria non e' accessibile: va chiesto all'area `COMP`.
