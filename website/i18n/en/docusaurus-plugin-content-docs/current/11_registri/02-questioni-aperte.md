---
title: Open questions
sidebar_position: 3
description: "The register of open questions between the project areas: what is still unresolved, who raised it, who must decide it and where it stands."
---

# Open questions

> **This chapter is generated.** Its source is the project's inter-agent noticeboard,
> projected into `registro/questioni.tsv` and from there into this page. Editing it by
> hand has no effect: the edit disappears on the next regeneration.

Each entry is titled with the **sigla alone**, so that a reference such as `#q-160` is
stable and identical in both languages. The **From** and **To** fields name the area that
issued the entry and the areas it binds; the area siglas are those used in the project's
internal working context.

**Entry text remains in the language in which it was issued**, Italian, including on this
English page. This is a declared limitation, not an oversight: translating a register entry
would mean keeping two versions aligned at every change to the noticeboard, and a translation
that drifts from a governance register tells readers of different languages different things
about what the project is bound to do.

### Q-01

**From**: `GUIDA` · **To**: `COMP` · **State**: OPEN

Scope boundaries to align with the declaration of intended use: no interpretive judgment in notices, no verification of drug interactions, no prognosis, no image enhancement

### Q-02

**From**: `GUIDA` · **To**: `ARCH` · **State**: **RESOLVED by `ARCH`** - **Neither of the two: structure in the product, content per tenant** ([`docs/adr/0018-catalogo-prestazioni-struttura-nel-prodotto.md`](../adr/0018-catalogo-prestazioni-struttura-nel-prodotto.md)). The product defines the *form* of an entry (code, catalog attribution domain, description, branch, enabled channels, temporal validity, reference to national code) and the operations of loading, validation, retrieval and deactivation; **content is tenant data**, loaded by documented interface, never in distribution. Rejection of inclusion: with independent cycles the project's release cadence would be determined by acts of others' administration, and the project would assume editorial responsibility for normative content of third parties. Rejection of referencing alone: without structure the product does not validate and does not make a service selectable, and each tenant reimplements. **Three binding consequences**: (a) **dual native coding**, tenant catalog code *plus* national code, because billing requires both - the service delivered remotely maintains the coding and fee of the corresponding in-person service and the modality is a channel modifier; (b) **mandatory temporal validity**, otherwise historical billing is irreproducible; (c) **the project distributes no catalog**, not even as example with real data. Rule extended by analogy to every reference datum with external lifecycle and territorially differentiated - **including the content of assessment instruments** (Q-11). Coordination note with `DOM`: the catalog carries **two taxonomic attributes, not one** (Q-147), and the structure defined here accommodates both

Is the service catalog included in the reference data or exclusively referenced by the tenant? The regional catalogs are 21 independent cycles

### Q-03

**From**: `GUIDA` · **To**: `ARCH` · **State**: **RESOLVED by `ARCH`** - **Two separate archives by construction, with three automatically verifiable rules** ([`docs/adr/0019-separazione-stringhe-di-interfaccia-ed-etichette-ufficiali.md`](../adr/0019-separazione-stringhe-di-interfaccia-ed-etichette-ufficiali.md)). (1) The **official label** lives in the label field of the coded concept and is populated **only by the terminology gateway**: no other path writes to it, and verification is blocking. (2) The **project string** lives in the internationalization archive, **indexed by the system plus code pair** and not by label, because two terminologies may use the same code with different meanings and label-based indexing would produce silent collisions. (3) **The interface never shows the official label field directly**: it asks the project archive and, if missing, falls back to the official label *declaring* that it is the original form and **signaling the gap** instead of masking it. **In output toward a third system the official label is always emitted, never the project string**: emitting the project's translation would mean producing and distributing a derivative assigned to the terminology owner. Process consequence to account for: translation gaps grow with the breadth of terminologies used and require a process, not an occasional activity

Separation between internationalization strings of the project and official `Coding.display`: how is it realized concretely

### Q-04

**From**: `GUIDA` · **To**: `SEC`, `ARCH` · **State**: **RESOLVED by `SEC`** - It is a third-party component **at runtime**, not a compilation dependency: falls into supply chain requirements and due diligence toward suppliers. Three consequences: (a) it cannot stand on the **main path** (V-03); (b) if established outside the EU, it is a **transfer** the moment it receives data referable to a patient - therefore queries **must never carry patient identifiers**, and sovereignty is satisfied **by absence of data**, not by location; (c) it becomes a **relevant second-level supplier** that the client must declare nominatively to the authority with the country of registered office. No persistent cache on disk. **Remains to `ARCH`** the choice of resolution point and the gateway contract. - **Residual part CLOSED by `ARCH`** ([`docs/adr/0016-gateway-terminologico-unico-e-disattivabile.md`](../adr/0016-gateway-terminologico-unico-e-disattivabile.md)): **single gateway**, no context interrogates a source directly, automatic blocking verification. Gateway contract: resolution, validation and expansion behind a stable interface that hides source diversity; **disactivation per coding system**, not global, as installation configuration; **source version registered together with the outcome** of every validation, because a non-repeatable validation is not evidence; **declared degradation** - if the gateway is unreachable the system **starts anyway** and dependent paths apply the configured policy (accept with non-validated marking, or refuse), never fail indiscriminately. The three consequences posed by `SEC` are fully accepted. Full functionality with resource-intensive systems disabled **is verified by executing the complete functional suite** with disactivation active (scenario SQ-07), not declared. Also accepted **V-157**: interrogation toward an external source passes through the unique egress mediator

Classify the terminology server as a third-party component and verify its compatibility with the sovereignty constraint

### Q-05

**From**: `GUIDA` · **To**: `INTEG` · **State**: **RESOLVED** - *response from `INTEG`*: **confirmed**. The SPID connector for the federation product configures the required authentication context (`authnContextClassRefs`, `authnContextComparisonType`) **statically on the single identity provider instance** (B7 §7.3.2, table *Requested AuthnContext Constraints*; B7 §7.7.2 point 2). A variable level per operation therefore requires **an instance for each pair (SPID provider x level)**. Impact confirmed on federation architecture, in four points: **(1)** the number of providers is read from the national registry and not hard-coded (B7 §7.3.2), so the multiplier acts on a set of variable cardinality over time; **(2)** the service provider's metadata document contains **an `AssertionConsumerService` for each configured instance** (B7 §3.3), so doubling the instances doubles the indices of the metadata **deposited with the authority**, and each variation entails a new deposit: it is procedural cost, not code cost; **(3)** organization data are taken from the **first instance in alphabetical order**, so the alias convention becomes a correctness constraint not a style one - the project adopts `spid-<NN>-<sigla>-l2` / `spid-<NN>-<sigla>-l3` with stable numeric prefix and continuous integration verification that generated metadata contains the expected values; **(4)** with `Comparison="minimum"` on the level 2 instance a higher-level credential **already satisfies** the request, so the second instance serves only where `exact` semantics are needed or a strictly higher level. **Perimeter decision `INTEG` for v1.0: two levels only** (level 2 as base, level 3 for tenant administration operations and configurations that require it), so the factor is **2**, not *n*. Toward the integrator **no interface impact**: what changes is that the propagated level is the one **required**, not the asserted one (B7 §4.3), as documented in [`docs/07_integration/06-identita-e-delega.md`](../07_integration/06-identita-e-delega.md). Residual verification remains transferred to **Q-160**

If the required authentication context is static per identity provider, do two instances serve per SPID provider: confirm the federation architecture impact

### Q-06

**From**: `GUIDA` · **To**: `ARCH`, `TECH` · **State**: **RESOLVED by `ARCH`** ([`docs/adr/0017-uri-del-codice-fiscale.md`](../adr/0017-uri-del-codice-fiscale.md)), and the response is written in full in the tail of this cell: the cell had remained open at the head while the closure had already been drafted, so the entry appeared open to whoever read only the first line. Aligned 26 August 2026 - **note from `TECH`, part of implementation**: the choice of canonical identifier is by `ARCH` and `DOM` and **is not anticipated here**. What this area can fix without overreach is the **technical form that makes the choice reversible**: (1) no external identifier is primary key - it lives in an identifiers table with `system`, `value`, `use` and temporal validity, never in a column of the main table (base architecture §3, [`docs/01_technical/03-persistenza.md`](../01_technical/03-persistenza.md) §1); (2) there exists a **versionable system identifiers registry** that declares, for each recognized identifier, whether it is **accepted on input**, whether it is **emitted on output** and with what precedence, and configuration is **per tenant**, because integrator and regional system may diverge; (3) translation happens **at the boundary**, once only, in the adaptation layer, not in the domain; (4) **all known forms are accepted on input**, **one only is emitted on output**, and emission is configuration, not code constant. With this structure the decision of `ARCH` becomes a configuration value and not a migration · **concurrent `PROTO`, without deciding**: the problem is documented with its measure in [`docs/04_protocols/02-fhir.md`](../04_protocols/02-fhir.md) §9.3, and **area protocol examples do not contain the tax code identifier**, so as not to anticipate a decision of others. Verified consequences: search by token that finds nothing, conditional creation that duplicates instead of recognizing, cross-validation that fails, consumer that reconciles on name and birth date. **Motivated recommendation of `PROTO`**: (1) the URI consistent with the declared conformity to the *Teleconsultation with Patient* family is the one used by that family and by the base guide; (2) projection toward the URI of *IT-Core* happens **at the boundary with the consumer**, per tenant configuration, without touching the internal model - consistent with the technical form already fixed by `TECH`; (3) translation is a conformity operation to be recorded in ADR, not silent rewriting; (4) **never** write both identifiers in the same resource: it worsens downstream deduplication instead of improving it; (5) divergence must be reported to the entity. **Distinct but connected point**: the identifier type code in the `PID-3` segment of the legacy channel (`04-hl7-v2.md` §4.3) remains **contractual with the integrator** - `NNITA` recommended as conforming to the table 0203 formation rule, `NN` alone **does not exist** - and the decision on Q-06 **does not resolve it automatically** · **RESOLVED by `ARCH`** ([`docs/adr/0017-uri-del-codice-fiscale.md`](../adr/0017-uri-del-codice-fiscale.md)): internal canonical value **`http://hl7.it/sid/codiceFiscale`**, consistent with the declared conformity to the *Teleconsultation with Patient* family and identical in the base guide. **Fully accepted the technical form of `TECH` and the recommendation of `PROTO`**, including point (4): **never both identifiers in the same resource** - the first draft of this area envisaged dual emission toward *IT-Core*-aligned consumers and **was corrected**, because redundant emission worsens downstream deduplication: a consumer that finds two may read them as two distinct identities, and the defect is silent and accumulates. Form adopted: **all known forms accepted on input, one only emitted on output**, projection toward the other URI **at the boundary with the consumer** per tenant configuration and destination, **versionable system identifiers registry** as the sole place where divergence is known, external identifier **never a main table column** but a row of an identifiers table with system, value, use and validity (incorporates **V-142** from `DOM`). Translation recorded as a conformity operation, not silent rewriting. Divergence openly declared in integration documentation and to be reported to the entity. Declared cost: an undeclared recipient receives the default value, so activation must **explicitly ask** which profile the recipient adopts

Verified divergence of the tax code system URI between Italian guides: which one is adopted and how is translation at the boundary managed

### Q-07

**From**: `GUIDA` · **To**: `COMP` · **State**: OPEN

CDA2 template, documentary codes and indexing metadata for the ten FSE types are not publicly available: who should be asked and within what timeline

### Q-08

**From**: ORCH · **To**: `ARCH` · **State**: **RESOLVED by `ARCH` - two-mode solution confirmed** ([`docs/adr/0014-due-modalita-di-sessione-media.md`](../adr/0014-due-modalita-di-sessione-media.md)). Incompatibility is not technically resolvable: a component that records must be able to decrypt, and a decrypted stream at an intermediate point is not encrypted to the endpoints. The hypothesis of declaring encryption to the endpoints anyway was discarded: it would be false, and it is the type of defect that destroys the credibility of the entire system. **Seven effects on the data model**: (1) the **mode is a state of the session aggregate**, not a boolean attribute of the recorded material; mutually exclusive states and transition traced with time and attribution; (2) the **recorded material is an autonomous aggregate** with reference to the manifestation of will, reference to the key, **validity always valued**, conservation state - there is no material without current consent and without expiration; (3) **encryption keys at rest per tenant**, never shared; (4) **revocation has immediate effect** on ongoing recording: *partnership* relationship between consent context and session context, not client-supplier, therefore not skippable; (5) **recording consent is granular per session**, not inheritable - consistent with **V-146** from `DOM`, making it one of five distinct objects; (6) **recorded material is not clinical documentation** and enters the documentary context only by explicit acquisition decided by the professional and recorded; (7) **playback is a traced operation** with its own severity. Obligations accepted: informative that explicitly declares the loss of encryption ownership; persistent indicator and **not themeable nor concealable** (V-163); container **negotiated at runtime** and **chosen based on actually negotiated codecs**, recorded in metadata (V-11, V-115); no static declaration of suite or protocol versions (V-156)

Session recording mode and encryption to the endpoints are incompatible: confirm the two-mode solution and its effects on the data model

### Q-09

**From**: ORCH · **To**: `ROAD` · **State**: OPEN

The deadline for adjusting security measures is subjective for each client: the roadmap cannot hard-code a single date. How is it parameterized

### Q-10

**From**: ORCH · **To**: `COMP` · **State**: OPEN

Artifact to applicable regime table: which artifacts fall under the cybersecurity regulation and which are covered by medical device discipline

### Q-11

**From**: `GUIDA` · **To**: `COMP`, `ARCH` · **State**: **OPEN for `COMP` - part `ARCH` closed with precautionary measure** ([`docs/adr/0024-punteggi-di-scale-cliniche-esclusi-in-via-cautelativa.md`](../adr/0024-punteggi-di-scale-cliniche-esclusi-in-via-cautelativa.md)). Pending closure: the model **represents and retains the response** to a structured questionnaire, with reference to an immutable version of the instrument, instant and respondent; **does not represent clinical scale scores and the telemonitoring context does not calculate them**; the **instrument content** - question text, options, algorithm - **is not in distribution** and is tenant configuration data, loaded by the installer, who is the subject having or able to obtain the license (same form as Q-02). The measure is **reversible without migration**: adding calculation on already-retained responses is possible, the reverse is not; realizing the calculation and verifying afterward would mean **removing a function already promised and already used**, with historical data calculated with an algorithm no longer executable. Criterion suggested to `COMP`: the same four-regime taxonomy of terminologies, applied **instrument by instrument** on the primary license and not on the container declaration. Two cautions: (a) if calculation is introduced the boundary with interpretive purpose must be evaluated, because a score calculated by the system is closer to that boundary than a retained response; (b) `FUNZ` has opened the block `RF-323`…`RF-332` on scores and scales: the precautionary measure **does not contradict it** but conditions its activation

**Validated clinical scales and questionnaires have their own licenses**: the terminology policy must be formally extended to scales and scores **before** writing the first calculation engine

### Q-12

**From**: `GUIDA` · **To**: `FUNZ` · **State**: **RESOLVED by `FUNZ`** - All 58 consequences now have an identifier, and the point-by-point mapping one by one is in [`docs/03_functional/02-catalogo-dei-requisiti.md`](../03_functional/02-catalogo-dei-requisiti.md) §13 (tables 13.1-13.4). **Eight new blocks** have been opened, frozen per `D45` and declared in **V-120**: `5.Q` pathway, enrollment and plan (`RF-230`…`RF-248`); `5.R` acquisition and measure quality (`RF-251`…`RF-266`); `5.S` evaluation, alarms and escalation (`RF-269`…`RF-290`); `5.T` silence, adherence and volume surveillance (`RF-293`…`RF-306`); `5.U` coverage and routing (`RF-309`…`RF-320`); `5.V` scores and scales (`RF-323`…`RF-332`); `5.W` reporting and near events (`RF-335`…`RF-340`); `5.X` feasibility and in-person fallback (`RF-343`…`RF-352`). Plus `RNF-090`…`RNF-110` and `BR-100`…`BR-185`. **The six uncovered areas are covered thus**: (1) *versioned plan* → `RF-233`, `RF-239`, `RF-244`, `RF-245`, `BR-101`, `BR-102`; (2) *waiting window* → `RF-293`…`RF-295`, `BR-150`, `BR-151`; (3) *escalation with declared failure* → `RF-281`…`RF-284`, `BR-134`, `BR-141`, with status `EX-TM-ESCFAIL` and the invariant that the alarm **remains open**; (4) *volume surveillance* → `RF-300`…`RF-303`, `RNF-092`, `BR-155`, with the requirement that detection precedes expiration of the first individual active window; (5) *declared service hours coverage* → `RF-309`…`RF-314`, `RNF-102`, `RNF-109`, `BR-160`, `BR-161`, made **activation condition of the plan**; (6) *calculation traceability* → `RF-324`…`RF-332`, `BR-120`…`BR-126`, with the eight mandatory elements of calculation. The ten dangerous use scenarios of telemonitoring are mapped in §13.5 **with the risk introduced by the mitigation**, for insertion in the risk file (salts with **Q-13**)

58 design consequences on chronicity, alarms and patient safety await requirement identifiers. At least six areas are uncovered: versioned telemonitoring plan, waiting window, escalation with declared failure, surveillance of expected volume, declared service hours coverage, calculation traceability

### Q-13

**From**: `GUIDA` · **To**: `COMP` · **State**: OPEN

Two dangerous use scenarios identified in the patient safety module must be inserted in the risk management file with gravity to be re-evaluated

### Q-14

**From**: `GUIDA` · **To**: `PROD`, `FUNZ` · **State**: **PART `FUNZ` RESOLVED, remains open toward `PROD`** - Fully accepted and translated into requirements: coverage is **runtime data versioned** per tenant and per pathway, with bands, days, holidays, response times per severity and **active recipients per band** (`RF-309`); it is **activation condition of the plan** - a plan without declared coverage does not activate, and no configuration can bypass the control (`RF-243`, `BR-160`); it is **visible persistently to patient and caregiver with current status**, not theoretical hours, together with the alternative channel, and **is not concealable by personalization** (`RF-310`, `RF-311`, `BR-161`, consistent with **V-163** from `INTEG`); **conditions the validity of the recipient in the escalation chain** - a recipient out of coverage is not a valid recipient and is skipped with reason recorded (`RF-281`, set as **V-122**); the event generated out of coverage is **marked `EX-TM-OUTOFHOURS` and queued with declared policy**, and never assumes a status implying a taking over occurred (`RF-312`); the patient nonetheless receives **immediate instruction** if the event arises from their action (`RF-313`, `RF-318`); **coverage reduction of an active service is a safety event** with reason, list of affected plans and notification to enrolled patients (`RF-314`); and is **verifiable retrospectively** if a missed response was expected or anomalous (`RNF-109`). Extended treatment in [`docs/03_functional/05-gestione-degli-allarmi.md`](../03_functional/05-gestione-degli-allarmi.md) §11. **Remains to `PROD`**: public communication must not present coverage as commercial service level nor as menu option, and material cannot declare broader coverage than presided - salts with **Q-121**

**Declared service hours coverage is a safety requirement**: a poorly declared service is more dangerous than service absence, because it produces false assurance. It must be treated as a requirement, not as a commercial parameter

### Q-15

**From**: `GUIDA` · **To**: `ARCH`, `PROTO` · **State**: **RESOLVED by `ARCH`** · **OPEN toward `PROTO`** for the residual declared at the tail of this cell ([`docs/adr/0021-convenzioni-delle-interfacce-pubbliche.md`](../adr/0021-convenzioni-delle-interfacce-pubbliche.md)); the closure was already drafted and the head of the cell was left open, aligned 26 August 2026 → **`PROTO` part discharged**: the ten choices are formulated, motivated and with the **declared cost** in the table `P-01`…`P-10` of [`docs/04_protocols/01-principi-di-interoperabilita.md`](../04_protocols/01-principi-di-interoperabilita.md) §5, each with a cross-reference to the chapter detailing it. Summary: **P-01** major version in path plus dated version header, with fixing on first call; **P-02** `428` on clinical writes lacking validator, declared deviation because the spec permits but does not require refusal; **P-03** «not found» instead of «forbidden» on resources referring to a patient, so as not to offer an enumeration oracle; **P-04** retention 24 hours of idempotency keys, scope `(tenant, client, operation, key)`; **P-05** dual emission of quota headers for a period **with published end date**; **P-06** twelve months notice with two blackouts scheduled at nine and eleven months; **P-07** load with references only (picked up as **V-135**); **P-08** exponential backoff with mandatory random variation, twelve attempts, approximately seventy-two hours; **P-09** major version in event type name; **P-10** introspection **only** on irreversible or high-impact operations. None is presented as conformity to a standard. **Formal decision and related ADRs remain with `ARCH`** · **RESOLVED by `ARCH` - cumulative ADR** ([`docs/adr/0021-convenzioni-delle-interfacce-pubbliche.md`](../adr/0021-convenzioni-delle-interfacce-pubbliche.md)): **the ten proposals from `PROTO` are adopted, with a single modification**. **Modification on P-05**: the **dual emission of quota headers is not adopted**, even with published end date. Reason: emitting a form **never standardized** legitimizes it with integrators and then makes its removal costly, and the compatibility benefit is lower than the cost of having taught a wrong form. **Only the current form is emitted** (incorporates C-03). The other nine are adopted as formulated, with the motivations of `PROTO` integrated by this area's: **P-02** deliberate deviation, because silent overwrite on clinical data is loss of health information; **P-03** limited to resources whose existence is itself information, not generalized, because elsewhere diagnosticability is worth more; **P-04** conservation **exceeding the maximum retry window**, otherwise a late retry finds the key expired and duplicates; **P-06** motivated by the profile of expected integrator, who with short notice does not migrate but remains blocked; **P-08** mandatory random variation so as not to produce, on reactivation, a synchronized burst against the integrator; **P-10** because an operation with long duration without progress is indistinguishable from a blocked one and the typical reaction is to re-launch it, which on a deletion is what must not happen. **Incorporated C-01, C-02, C-04, C-06, C-10** and constraints **V-110**, **V-130**, **V-126**. `PROTO` may now document them as decisions · **Residual open toward `PROTO`, verified on text 26 August 2026**: the row `P-05` of [`docs/04_protocols/01-principi-di-interoperabilita.md`](../04_protocols/01-principi-di-interoperabilita.md) §5 still declares the **dual emission** of traffic quota headers («Yes, for a declared period: current form plus deprecated marked form»), which is **exactly the sole proposal not adopted** by `ADR-0021`. As long as that row remains as is, the principles document and the decision register say the opposite to each other on the same convention, and the integrator who reads the first learns a form that the project has decided not to emit. In the same act must be incorporated the other three corrections listed in [`docs/02_architecture/09-decisioni-rinviate.md`](../02_architecture/09-decisioni-rinviate.md) §3 `B-5`. **The correction belongs to the protocols area and this pass does not execute it**

Ten choices stated as project proposal deserve an ADR: version in path, precondition code on clinical resources, not found instead of forbidden, idempotency key retention, dual emission of quota headers, twelve months notice of dismissal, lean payload in webhooks, retry policy, event type versioning, introspection on high-impact operations

### Q-16

**From**: `GUIDA` · **To**: `SEC`, `TECH` · **State**: **RESOLVED by `TECH` for the implementation part** - Accepted. Implementation: a single component in `platform` traversed by **every** outgoing call toward an external origin address (webhook destinations, terminology gateway, integrator systems, verification key material retrieval, referenced resource recovery). The component resolves the name, evaluates the resolved address and **repeats evaluation after each redirection**, because name evaluation is bypassable. No outbound endpoint can construct an autonomous HTTP client: the prohibition is verified by static analysis and by dependency rule G13 of the pipeline. **Two cautions that this area adds on the basis of relay node verification**: (a) the same family of defects - circumvention of prohibited address lists by canonicalization or incorrect IPv6 form comparison - has produced **six distinct vulnerabilities in eight years** on a mature component, four of which in the last eight months; (b) it follows that **application filtering is defense in depth and not primary defense**: primary defense is the **outbound network isolation** of the workload, applied by infrastructure. Documented in [`docs/01_technical/05-media-e-tempo-reale.md`](../01_technical/05-media-e-tempo-reale.md) §4.4 and `08-quality-and-testing.md` §3.3. **Remains to `SEC`** the definition of prohibited ranges and their maintenance · **CLOSED by `SEC`** - Accepted and completed as **V-157**, making it an architectural requirement with **network route negation at the network level** to application components, not a coding rule verified by convention. Order of mediator controls, list of five exit points, motivated exclusion of relay and single abuse test suite in [`docs/06_security/06-sicurezza-applicativa.md`](../06_security/06-sicurezza-applicativa.md) §8. **Prohibited ranges** (definition from `SEC`): return interface; private address spaces; link-local addresses; **infrastructure metadata service address**; **public address of the node itself**; IPv4 mapped in IPv6; transition prefixes; documentation and test spaces; multicast and broadcast. **Three maintenance rules**: (a) all IPv6 ranges are **aligned to a prefix**, as required by the mitigation declared by the component-per-component comparison defect notice; (b) **the allowed address list is not used** in the health profile, because it always prevails over the prohibited one and a single permissive row nullifies the entire denial; (c) the list is **versioned with the code** and reverified at every minor version update of the relay, with outcome recorded in the surveillance file. Consistency note: this area **confirms** the caution from `TECH` - application filtering is defense in depth, primary defense is network isolation (V-10, V-157)

Protection against requests addressed to internal resources must be implemented **once only** in a shared component, not repeated for every exit point

### Q-17

**From**: `GUIDA` · **To**: `TECH`, `COMP` · **State**: **RESOLVED by `TECH`** - Accepted as **base of departure**, with a method precision that changes its use: the protocols table is an inventory of **specifications**, while the inventory required by IEC 62304 §8.1.2 is an inventory of **implementations** - it is the library in an exact version, not the standard, that has a license, a notice channel and a correction history. The adopted register is therefore populated **per generation** from the bill of materials produced by the pipeline (D45: from the first pipeline), and enriched with fields the bill does not contain - function in system, known alternative, impact on risk, update service level, review date - via a versioned annotations file. The protocols table enters as **source of the «function in system» field and specification to implementation mapping**, avoiding reconstruction. The G5 control of the pipeline **makes construction fail** if a component appears in the bill and not in the annotations: it is the mechanism that prevents entry of an unevaluated dependency. Registry schema in [`docs/01_technical/01-stack-e-motivazioni.md`](../01_technical/01-stack-e-motivazioni.md) §14; mechanism in `09-continuous-integration-and-release.md` §§3-4

The summary table of protocols is in fact an **inventory of starting point for third-party components**: must be used as a base instead of rebuilding it

### Q-160

**From**: `INTEG` · **To**: `ARCH`, `TECH` · **State**: OPEN

Residual of Q-05: **it is not verified** whether the federation product, acting as client toward an external identity provider, **forwards the required level parameter** through the intermediation realm (B7 §7.7.2 point 1). If it does not forward it, level elevation per operation is not obtainable by configuration alone and an extension is needed. Empirical verification at nearly zero cost, to put on the critical path **before** declaring in public documentation how assurance level is propagated

### Q-161

**From**: `INTEG` · **To**: `ARCH`, `SEC` · **State**: **RESOLVED by `ARCH`** ([`docs/adr/0029-registro-di-fiducia-unico-per-tenant.md`](../adr/0029-registro-di-fiducia-unico-per-tenant.md)): the trust registry is **unique per tenant**, and is the sole source of truth - not necessarily a single table: operational lists of individual components are **derived**, never hand-written, and a derived list that diverges from the source is a detectable defect. Confirmed what this entry asked: the same set feeds permitted issuers and public key material addresses, origins for embedding, origins for cross-origin sharing and outbound message destinations. Discarded the **undifferentiated single list**: **each entry declares the capabilities granted**, with temporal validity, because trusting a subject as identity issuer is not trusting him as destination of outbound messages. **Same object of `Q-156`**, to be read together: the two had remained in different states, and it is the divergence that this pass closes

The trust model toward the integrator is **per tenant**: permitted `issuer`, `jwks_uri` in allowed list, permitted algorithms, expected `aud`, claim mapping. Confirmation needed that this register is **unique** and shared between federation, allowed origins for embedding, allowed origins for cross-origin resource sharing and allowed destinations for webhooks. Three separate registers always diverge

### Q-162

**From**: `INTEG` · **To**: `COMP` · **State**: OPEN

The document [`docs/07_integration/09-obblighi-di-chi-integra.md`](../07_integration/09-obblighi-di-chi-integra.md) states that who integrates and puts into service assumes the manufacturer role under D28/D49 and that **responsibility allocation must be formalized contractually**. A review by `COMP` of the allocation table (controller/processor, manufacturer, identity service provider, NIS subject) is needed before publication

### Q-163

**From**: `INTEG` · **To**: `FUNZ`, `PROD` · **State**: **CONFIRMED by `FUNZ`** - Confirmed without reservation, and made a verifiable rule. `BR-170` forbids any functionality from mediating the access of an insurer, surveyor or employer to the file, **neither directly nor through a professional**, with consequence «refusal and safety event of critical severity». `BR-171` separates **payment title** and **access title** as distinct objects with distinct legal bases: the payer is not a consultant, and the use case charged to funds, mutuals and policies remains valid for **delivery alone**. `OUT-18` registers it as category `NORM` perimeter exclusion, not reopenable as long as the source is in force, with the indicated verification: negative proof on role composition **and** on documentary access. No typed outcome in the catalog ([`docs/03_functional/03-casi-d-uso.md`](../03_functional/03-casi-d-uso.md) §4) exposes clinical content: billing outcomes carry service code, channel attribute and administrative effect. Consistent with **V-166** from `INTEG`

Corollary of **V-08**: the catalog of public events and authorization scopes **must not contain** any capability that enables a payer to obtain clinical content. Functional confirmation needed that the service completion event, in the billing variant, carries **only** service identifier, administrative outcome and amount, never references to clinical documents

### Q-110

**From**: `TECH` · **To**: `ARCH` · **State**: **RESOLVED by `ARCH` - third option: deterministic routing to owning node** ([`docs/adr/0012-segnalamento-fuori-dal-piano-degli-eventi.md`](../adr/0012-segnalamento-fuori-dal-piano-degli-eventi.md)). Distributor without persistence is **excluded** as `TECH` indicates and as **V-131** imposes. Balancer affinity is **permitted only as declared technical debt with written exit strategy**, for a reason beyond those already listed: **the two participants in the same session can end up on different nodes**, so the shared state problem remains entire and is not solved but moved. With deterministic routing the state machine lives in a single process and ordering is guaranteed because state is in a single place. Both technical constraints are incorporated as binding parts of the decision: exactly-once delivery and in order per session; **gradual drainage** - a node updating stops accepting new sessions and awaits conclusion of active ones within a declared window. **Declared fault mode and not hidden**: node failure terminates hosted sessions, which are re-established with renegotiation; migrating state would cost more than renegotiation. Related decision: **signaling does not transit through the outbox or the broker**, with automatic blocking verification

**Signal topology on multiple instances.** Three options with different costs: session affinity to balancer (simple, but redeploy truncates hosted sessions and resizing does not redistribute), distributor between instances (to **exclude** in the form without persistence: incremental collection of candidates requires exactly-once delivery and in order, which a publish-subscribe mechanism does not guarantee under reconnection), deterministic routing of session to owning node by consistent hashing (eliminates distributor from critical path and keeps state machine in a single process). It is a **structural decision**, with effects on distribution, update without interruption and fault modes: falls to `ARCH`, with ADR. Technical constraint that any choice must satisfy: **exactly-once delivery and in order, per session**, and gradual drainage sufficient not to truncate an ongoing session

### Q-111

**From**: `TECH` · **To**: `ARCH` · **State**: **PARTIALLY RESOLVED by `ARCH`** ([`docs/adr/0028-limite-dichiarato-di-partecipanti.md`](../adr/0028-limite-dichiarato-di-partecipanti.md)). **Decided - the topology**: the session remains **a mesh without central component**; no component terminating encryption is introduced in the current version solely to increase participants. The analysis by `TECH` is accepted without reservation: it is a security decision before engineering, of the same nature as the one on server-side recording, and introducing it would give the system a **third operating mode** with different properties. **Decided - the form**: the limit is **declared in documentation and enforced by code**, with clear error to the exceeding participant explaining the reason and indicating the alternative; it is **configuration with an imposed maximum**, not code constant - a tenant can lower it, never raise it. **Deferred - the number** `[NV]`: depends on the transmission bandwidth budget of the worst-connected participant, to be measured on the reference device and network **not yet declared** (Q-115). Declared criterion: the maximum is the number beyond which the reference participant does not handle their own budget **with margin**, measured and not estimated. Setting it before measurement would mean writing it by preference. **Dependency to report, not to discover**: if measurement excluded the third participant, the declared alternative measure for non-conformity of accessibility on subtitles - the interpreter - would need re-examination

**Declared limit of media session participants.** Mesh topology without central component is the only one preserving end-to-end encryption; beyond a certain number the transmission budget per client does not hold and every alternative introduces a component that **terminates encryption**, which is a security decision before engineering. **An explicit limit is preferable to silent degradation**: the number and the declaration form fall to `ARCH`

### Q-112

**From**: `TECH` · **To**: `COMP` · **State**: OPEN

**License regime of the time series extension.** Advanced functionality results - compression, continuous aggregations - not to be distributed under approved open source license but under proprietary source-available license, with restrictions on offering as managed service to third parties. `[NV]` - **to be verified by `COMP` on the primary license text, artifact by artifact** (D34: the container declaration does not dispose of rights to the contents). The project **does not redistribute** the extension and a designed fallback exists on native declarative partitioning, proved in continuous integration; remains to place the component in the regimes of D31 and to produce the text for the deployer

### Q-113

**From**: `TECH` · **To**: `COMP`, `ROAD` · **State**: **PARTIALLY DISCHARGED by `COMP`, remains open toward `ROAD` and the client** - Analysis accepted without reservation: **a commitment expressed in months is meaningless** for a component receiving fourteen releases in slightly more than seven months. **Formal placement confirmed**: the commitment belongs to the **post-commercialization surveillance plan** (art. 84 and Annex III MDR) and the **declared support period**, which is `Q-155` and remains with the client. **Structure this area fixes without overreach**: (a) evaluation **initiated within the business day following** notice publication; (b) **motivated decision recorded also when it is not to update** - clause 7.1.2 of ISO/IEC 81001-5-1 requires *evaluation*, not always correction, and an unevaluated update introduces more risk than it removes; (c) **remedy window differentiated by severity**, with the numeric value declared by the **manufacturer** and not the project, because it is a commitment toward third parties the project cannot assume (`D49`, `V-06`). **Added caution**: a security notice on a component participating in a risk control measure is **simultaneously** an IT security event, with hour-level terms, and potentially **a field safety corrective action**, with day-level terms; the process must produce **a single evaluation with two outputs**, not two parallel evaluations diverging. [`docs/08_compliance/08-sorveglianza-post-commercializzazione.md`](../08_compliance/08-sorveglianza-post-commercializzazione.md) §6

**Update service level of third-party components, expressed in days from notice publication and differentiated by severity.** A commitment expressed in months is meaningless: the relay node had **fourteen releases in slightly more than seven months**, five in August 2026 alone, with a critical vulnerability fixed mid-path. The technical proposal exists; the formal commitment belongs to the post-commercialization surveillance plan and the declared support period (connects to Q-155 from `SEC`)

### Q-114

**From**: `TECH` · **To**: `COMP` · **State**: OPEN

**Three technical compromises with clinical consequence, to be inserted in the risk management file.** (1) **Quality thresholds and notice to professional**: on exceeding the unsuitability threshold the system informs that conditions might not be suitable for the evaluation in progress and offers deferral - it is a risk control, and thresholds remain product specification and never conformity (V-12). (2) **Jitter buffer objective**: it is the only application leverage on the dominant contribution to latency, and lowering it reduces delay **at the cost of increased audible loss** under high jitter. (3) **Degradation preference**: preserving resolution or fluidity has different clinical consequences per specialty, and the defensible formulation under V2 is **rendering preference chosen by user**, not automatic adaptation guided by clinical content

### Q-115

**From**: `TECH` · **To**: `PROD`, `FUNZ` · **State**: OPEN - **note from `FUNZ`, without deciding**: device choice remains with `PROD`, but this area **constrains how the choice is used**. The acceptance criterion operative of `D25` is translated into two mandatory tests for every critical path ([`docs/03_functional/06-accessibilita-e-usabilita.md`](../03_functional/06-accessibilita-e-usabilita.md) §2): **degraded mobile test** and **test from keyboard with assisted reading**, both with recorded outcome. The critical paths on which they are mandatory are ten, and four are introduced by this area because the patient executes them **alone, every day, with nobody alongside**: manual entry of a measure, questionnaire completion, reading routing instruction, declaration of unavailability. Two requirements depend directly on the device `PROD` will choose: `RNF-106` (≥ 90% of representative participants complete measure entry on first attempt without assistance, on low-end device and limited network) and `RNF-107` (measure entered without connectivity is not lost). **As long as the device is not declared, `RNF-106` is not verifiable**: it is the dependency to report in the verification plan

**Reference device and reference viewport for mobile-first criteria.** Criteria M1-M8 of [`docs/01_technical/04-frontend.md`](../01_technical/04-frontend.md) §6 and weight and first-interaction time thresholds are verifiable **only** against a declared device. The device is not the developer's: it is a mid-range apparatus of a few years past, which is what the reference population holds. The choice is product's; measurement and publication of thresholds are this area's. `[NV]` - no numeric threshold is today declared, because none has been measured; the measurement falls to `TECH` once `PROD` has chosen the reference device

### Q-116

**From**: `TECH` · **To**: `COMP` · **State**: **CLOSED by `COMP`** - The retention term of diagnostic signals is **determined by device surveillance obligations, not operational need**, and **is distinct** from the terms of `V-152`, as this area had correctly indicated. **Determination**: the minimum is derived **by composition and not by choice** - maximum realistic latency between event and user report of it, plus the contractual term within which the user is bound to report to the manufacturer, plus the investigation time needed to integrate the initial report. **Motivated proposal: twelve months configurable** for signals referable to a service or alarm - not for general application logs - with **final determination by the manufacturer**, because it depends on the contractual term the manufacturer imposes on users. `[NV]` on value: **no source fixes a term for diagnostics**, and the project does not invent one presenting it as obligation. The requirement is set as **`V-178` letter (b)**, together with the twin capability the question did not identify: without a **stable taxonomy of counted events** retention alone is not enough, because the report of trend per art. 88 MDR requires counting non-serious events in a comparable way across installations and over time, and a renamed or consolidated category **makes increase disappear instead of detecting it**. Treatment in [`docs/08_compliance/08-sorveglianza-post-commercializzazione.md`](../08_compliance/08-sorveglianza-post-commercializzazione.md) §§5.2 and 8.2

**Retention of observability signals and incident reconstruction window.** Technical constraint: retention must be **at least equal to the time within which a report can arrive**. If an incident can emerge weeks later and diagnostic logs last days, reconstruction is impossible and the surveillance obligation is not satisfiable. Determination of the term depends on device surveillance obligations and IT security reporting requirements and falls to `COMP`; this area derives the configuration. Distinct from the terms already fixed by constraint V-152 from `SEC`, which concern traceability and access data, not diagnostics

### Q-150

**From**: `SEC` · **To**: `ARCH` · **State**: **RESOLVED by `ARCH` - the question is malposed and the answer is «all four»** ([`docs/adr/0013-registro-immutabile-a-quattro-strati.md`](../adr/0013-registro-immutabile-a-quattro-strati.md)). They are not alternatives: are **layers covering different threats**, and each has a "does not cover" column that the others fill. The **application chain** covers modification, deletion, reordering and retroactive insertion but not wholesale rewrite by whoever controls the application; the **append-only archive** covers modification and deletion within the retention period but not failure to write an entry never produced; **storage with separate privileges** covers rewrite by the application archive administrator but not collusion; **periodic anchoring signed and timestamped** covers collusion within the interval. With a single technique only its column is accepted: with application chain alone the guarantee reduces to **trust in the operator**, which the requirement excludes. **Declared residual vulnerability window**: entries between two anchors, in the sole hypothesis of collusion between the two administrators. Other choices: chain **per tenant and not global**, because a global chain would make it impossible to deliver to a controller evidence of only their accesses without exposing them to the existence of others; **write to primary archive blocking** - its failure fails the application operation, with the accepted consequence that register unavailability is unavailability of the system for clinical operations - and **async replica** with watched delay; **the chain does not repair itself**, on breakage a new generation opens anchored to the previous and breakage itself is recorded; **calculation method documented publicly**, because secrecy adds no security and subtracts verifiability. **Remain to `SEC`**: interval and threshold value, timestamp service, archive retention, verification cadence, minimal form of request origin, and whether the register archive may be the same engine with separate privileges - the latter affects weight at customer installation and this area signals that **the requirement is privilege separation, not engine separation**

ADR on immutable log: application hash chain, append-only archiving, single write on object, or periodic signature with timestamp

### Q-151

**From**: `SEC` · **To**: `COMP` · **State**: OPEN

The detailed annexes of the authority determination have not been read line by line: **no punctual requirement of the 43 measures must be cited in public documentation** before they have been read

### Q-152

**From**: `SEC` · **To**: `ARCH`, `ROAD` · **State**: OPEN - **`ARCH` fixes what must be monitored, not with which threshold** ([`docs/02_architecture/08-viste-di-deployment.md`](../02_architecture/08-viste-di-deployment.md) §8; deferred decision B-6). Three quantities are **architecturally mandatory** because their absence renders silent faults invisible: **depth of the queue of unprocessable messages**, **latency of the outbox relay**, **outcome of register integrity checks**. A system that does not expose them can be severely broken and appear healthy. The **objectives of recovery point for category** must further be distinguished: **zero** for signed documentation, consents and register - which implies synchronous replica for that category, with the cost in latency on the signing operation **declared to the user** and not hidden by optimistic confirmation that might prove false; brief and declared for services and agenda; tolerated loss for channel metrics. Incorporated **V-113** and **V-114**: no raw cumulative counter is citeable as indicator, and the synthetic index is proprietary and must be declared such. **Terminology note**: in this area's documentation «surveillance» referred to components means exercise observability; referred to the patient the forbidden formulas from **V-144** do not appear

Expected service levels for purposes of continuous monitoring, distinct from those provided by the decree on regional infrastructures

### Q-153

**From**: `SEC` · **To**: `INTEG` · **State**: OPEN

Empirical verification of forwarding the required authentication context through the broker and its translation in the federation protocol

### Q-154

**From**: `SEC` · **To**: → **ORCH** · **State**: OPEN

If the managed service operator exceeds medium-enterprise thresholds **becomes a NIS subject in their own right**: client decision

### Q-155

**From**: `SEC` · **To**: → **ORCH** · **State**: OPEN

Active monetization activates the manufacturer role under the cybersecurity regulation; the **declared support period**, minimum five years, must also be decided

### Q-156

**From**: `SEC` · **To**: `ARCH` · **State**: **RESOLVED by `ARCH`** ([`docs/adr/0029-registro-di-fiducia-unico-per-tenant.md`](../adr/0029-registro-di-fiducia-unico-per-tenant.md)). **The response was already drafted** - it is written in full at the tail of the question cell, where whoever resolved placed it - but the status cell remained `OPEN`, so the entry appeared open to whoever read only the status column. Aligned 26 August 2026, together with `Q-161`, which has the same object

**The trust registry per tenant must be unique.** The same set must feed permitted issuers and public key material addresses, allowed origins for embedding, origins for cross-origin sharing, webhook destinations and **allowed list of the egress mediator** (V-157). Three separate registers diverge, and **divergence always favors who attacks**: an origin removed from one list and not the other remains valid on the second. **RESOLVED by `ARCH` - unique per-tenant registry, with explicit capabilities per entry** ([`docs/adr/0029-registro-di-fiducia-unico-per-tenant.md`](../adr/0029-registro-di-fiducia-unico-per-tenant.md)). Fully accepted the argument from `SEC`: divergence always favors who attacks. Decided form: **a single source of truth per tenant**, not necessarily a single table - operational lists of individual components are **derived**, never hand-written, and a derived list diverging from the source is a detectable defect. Discarded **undifferentiated single list**: trusting a subject as identity issuer is not trusting him as destination of outbound messages, so **each entry declares the granted capabilities** from the five families, with temporal validity. **Atomic revocation on the source**, with propagation to derived lists at **declared and monitored delay** - an unmonitored delay is an invisible residual validity window - and invalidation of memorizations instead of awaiting expiration. Two constraints added: the link **main caller → tenant → trust anchor is the only validation path**, otherwise integrator A presents an assertion issued by integrator B's identity provider; and **the public key material address is never followed blindly** - is compared to the one registered for that subject and retrieval still passes through the unique egress mediator. Declared cost: the registry is critical component needing redundancy. - *original text from `SEC`*: this area fixes the requirement and **does not decide the form**: one table, one configuration, one check point. Salts with `Q-161` from `INTEG`

### Q-157

**From**: `SEC` · **To**: `FUNZ`, `COMP` · **State**: **PART `FUNZ` CONFIRMED, remains open toward `COMP`** - Confirmed on the functional side: the capability of selective audio-video obscuring **is not in v1.0 scope**, and is registered as `SCOPE` category exclusion - therefore reopenable by normal product governance and not by regulatory assessment. Two elements this area adds that reduce the problem frequency at the root: (a) recording is **exception not rule**, disabled by default at every level and with specific per-session consent, so the number of recordings containing third-party data is structurally low; (b) for service types marked non-recordable the function is **absent and not disactivable even by an administrator** (`BR-075`). The interim measure proposed - release of audio only from the requesting party - is **functionally compatible** with the model, because recording is already treated as an object with its own access, high-severity audit and dedicated clinical permission (`RF-144`, `RF-147`), and export requires justification and produces an encrypted file with registered recipient and purpose. **Remains to `COMP`** to establish whether partial release satisfies the interested party's instance or whether case-by-case assessment must anyway be documented

**Selective audio-video obscuring for right of access purposes.** A consultation recording contains data of a third party: art. 15, par. 4, prevents the copy from harming others' rights, but does not permit refusal without evaluation. No technically simple obscuring solution exists. This area declares the capability **not present in v1.0** and proposes as interim measure the release of the audio only from the requesting party: functional and conformity confirmation is needed, or the decision to design the capability

### Q-158

**From**: `SEC` · **To**: `ARCH` · **State**: **STRUCTURE DECIDED by `ARCH`, numeric value deferred to `SEC`** ([`docs/adr/0013-registro-immutabile-a-quattro-strati.md`](../adr/0013-registro-immutabile-a-quattro-strati.md)). **Dual triggering, not just temporal**: anchors on whichever comes first of declared interval elapsed and exceeding a **volume threshold of entries** - time-only triggering leaves many-entry intervals during peak hours and empty anchors overnight, that is concentrates risk where activity is highest. **Also anchoring on event**, before every operation that might alter the chain or its context: application of a retention policy with segment removal, register migration, tenant dismissal, selective recovery - anchoring before makes those operations non-repudiable. **Three cumulative destinations of attestation**, in force order: (1) archive of anchors with separate privileges, which alone does not protect from collusion; (2) **delivery of attestation to the data controller** together with the periodic report - is the strongest measure at nearly-zero cost, because from that moment a copy of the hash is **in the hand of a subject the operator does not control** and rewriting history means contradicting a document already delivered to a third party; (3) timestamp from qualified service, **configurable and not mandatory**, because it introduces an external dependency the customer installation might not have - when absent the declarable assurance is reduced and the reduction must be declared. **Remain to `SEC`** the value of interval and threshold, which are risk parameters

**Point and periodicity of external anchoring** of the cumulative register hash. The hash chain does not protect from wholesale rewrite by whoever controls the system: the rewritable interval is the one between two anchors. Salts with Q-150

### Q-159

**From**: `SEC` · **To**: `COMP` · **State**: OPEN

**Allocation of roles between data controller, data processor, manufacturer, identity services provider and obliged entity, to be reviewed before publication.** It also determines who counts as the clock in incident scenarios. (Identifier assigned on 27 August 2026: the issue was cited in the corpus as number twenty, predating the globalization of numbering and registered nowhere; the formulation is reconstructed from [`docs/06_security/09-ripartizione-delle-responsabilita.md`](../06_security/09-ripartizione-delle-responsabilita.md) §13, which lists five roles, and from [`docs/06_security/10-risposta-agli-incidenti.md`](../06_security/10-risposta-agli-incidenti.md) §13, which lists four, omitting the identity services provider: the divergence is declared here and not smoothed over.)

### Q-24

**From**: `COMP` · **To**: → **ORCH** · **State**: OPEN

The "artifact → applicable regime" table required by D41 does not yet exist and must be produced as an ADR

### Q-25

**From**: `COMP` · **To**: `ORCH` · **State**: **RESOLVED** - the document has been completed (sections 0-15) and now includes clinical evaluation, usability engineering, lifecycle safety, surveillance and vigilance, modifications, regulatory officer, costs and figures. The compliance area is derived from it: §8, technical file checklist mapped to project documents, is the source of [`docs/08_compliance/04-fascicolo-tecnico.md`](../08_compliance/04-fascicolo-tecnico.md); §§6-7 feed chapter 03 and §13 of chapter 04. **Remaining `[NV]` from B9 and not to be remedied by inference**: precise numbering of certain sections of Annexes I, IX and XI, designation codes of the implementing regulation, harmonization status of rules on information provided by the manufacturer and on symbols, article of the national decree on the obligation of Italian language

The research document on the certification pathway is **truncated**: it stops at section 9.4 but refers internally to sections 10-16 (clinical evaluation, usability, regulatory officer, national compliance, modifications, claims, costs). It must be completed before deriving the compliance area from it

### Q-26

**From**: `COMP` · **To**: `ORCH`, `GUIDA` · **State**: OPEN - the status was entirely missing from this line and has been restored on 27 August 2026 based on `.telemedic/piani/T-07-sito-di-documentazione.md`, which ascertains that the check of reachability of absolute references, which closes it together with `Q-198`, does not yet exist

The link to `NOT-A-MEDICAL-DEVICE.md` exits the `docs/` folder: with the site generator it must be replaced with an absolute address or the document must be duplicated inside `docs/`. **Blocking before first deploy** OPEN - **note `GUIDA`**: the problem **does not concern a single link**. The foundations guide exits `docs/` in at least four points - `CONTRIBUTING.md`, `NOT-A-MEDICAL-DEVICE.md`, `THIRD-PARTY-TERMINOLOGY.md`, `SECURITY.md` - and the references are **substantial**: module 00 declares the guide a prerequisite of `CONTRIBUTING.md`, and module 17 §2.2 makes the reading of the three rule documents a **prerequisite step of first startup**. Duplication inside `docs/` is therefore to be excluded: two copies of a document that declares intended use and limits of use are **two documents that will diverge**, in a context where documentary divergence is a regulatory defect - it is the same reason why check G8 exists. Recommendation from `GUIDA`: **absolute address to the repository**, with reachability verification inside G9. **The decision remains with `ORCH`** · **note `COMP`**: the chapters `00-indice.md` and `04-fascicolo-tecnico.md` of the compliance area already adopt the **absolute address** to the repository for references to `NOT-A-MEDICAL-DEVICE.md`, `DISTRIBUTION-POLICY.md`, `CONTRIBUTING.md`, `GOVERNANCE.md`, `SECURITY.md` and `THIRD-PARTY-TERMINOLOGY.md`. This area **excludes duplication inside `docs/`** with its own argument, which adds to the one from `GUIDA`: the declaration of intended use and limits of use is regulatory material within the meaning of art. 2, point 12, of the MDR, and **two divergent copies of regulatory material are a misleading declaration within the meaning of art. 7**, not a documentary maintenance defect

### Q-130

**From**: `PROTO` · **To**: `ARCH` · **State**: **RESOLVED by `ARCH` - affinity to the node that owns the session, for deterministic routing** ([`docs/adr/0012-segnalamento-fuori-dal-piano-degli-eventi.md`](../adr/0012-segnalamento-fuori-dal-piano-degli-eventi.md)), not ordered queue by key. Both are compatible with the specification; the choice is motivated by three elements: (a) the ordered queue places a persistent intermediary **on the critical path** of a flow with a latency budget in fractions of a second; (b) a blocked event blocks the entire key, and on an ongoing negotiation this translates to a session that does not establish; (c) with deterministic routing the ordering is not guaranteed by a mechanism, it is a **consequence** of the fact that the state is in a single process. Accepted and declared operational cost: the failure or update of a node terminates the hosted sessions, mitigated by the **gradual draining** set as a requirement. **V-131 is adopted as an explicit constraint of the ADR**, not as a note

Follows from **V-131**. How is reporting session state distributed across multiple nodes while respecting exactly-once delivery of candidates and in the same order: **connection affinity to the node that owns the session**, or **ordered queue by session key**? Both are compatible with the RFC, have different operational costs, and the choice is not `PROTO`'s. The defect produced by the wrong choice is intermittent and very difficult to diagnose: the session establishes nine times out of ten

### Q-131

**From**: `PROTO` · **To**: `DOM`, `COMP` · **State**: **Part `DOM` RESOLVED, remains open toward `COMP`** - *response `DOM`*: **proposal accepted in full** and adopted in [`docs/05_domain/04-documenti-clinici.md`](../05_domain/04-documenti-clinici.md) §6.4 (`DM-44`) with the four-row mapping: (a) connection quality measures and suitability certification → **"Descriptive parameters of the procedure"**, primary location; (b) synchronicity, patient presence, channel actually used and any fallback → **"Procedure execution methods"**; (c) identification and version of the platform → **"Instrumentation used"**; (d) presence of caregiver, another physician or healthcare operator with the patient → **"Other technical figure involved in procedure execution"** plus notes. **The constraint set by `PROTO` is accepted and strengthened**: the value is measured by the system and **confirmed by the professional**, never autonomously generated; the content is produced in **structured and repeatable form**, not as free prose. Reason for choice: the "descriptive parameters of the procedure" are the only field in the format whose semantics allow measured quantities without forcing, and keeping the certification separate from the "execution methods" allows qualifying the act even when quality was judged suitable. **Remains to `COMP`** the formalization in ADR and the conformity check (`REQ-46` of `B1`)

The ministerial format of the teleconsultation report (DM 19 November 2025, Ann. 1, § 2.20) **does not contain a field for certifying the quality of the connection and confirming suitability** required by Agreement 215/CSR 2020. `PROTO` **proposes** the "descriptive parameters of the procedure" as the primary location, with "procedure execution methods" to qualify synchronicity and patient presence and "instrumentation used" for platform and version. **Constraint that `PROTO` sets anyway**: whatever the field, the value is **measured by the system and confirmed by the professional**, never autonomously generated and inserted into the document - it would be information produced by the system inside a clinical document, in direct tension with **V2**. Decision and ADR to `DOM`/`COMP`

### Q-132

**From**: `PROTO` · **To**: `COMP`, `DOM` · **State**: OPEN - *note `DOM`*: **not performable in this area** because it requires reading the published profile, which has not been done. `DOM` provides however **the reference side of the comparison**: the **canonical dataset** is modeled field-by-field from the information set of Annex 1 § 2.20 in [`docs/05_domain/04-documenti-clinici.md`](../05_domain/04-documenti-clinici.md) §6.3, with the three-level structure of `DM-43` (domain → canonical dataset → serializations). **The coverage verification must therefore be conducted between canonical dataset and profile, not between domain and profile**: it is literal application of `V-07`, and has the practical consequence that a deviation is resolved in the serialization without touching the domain. Also flagged are two fields that **the format does not provide** and that must be considered in the analysis because they are mandatory content from another source: connection quality certification and participating collaborators (Q-131)

**Field-by-field coverage verification** between the information set in Annex 1 § 2.20 (binding source) and the `CompositionRefertoTelevisita` 0.2.0 profile (representation). **Not executed.** Where the profile has no place for a format field, a declared extension or motivated placement is needed. It is analysis of deviations to be put into work, not documentary verification

### Q-133

**From**: `PROTO` · **To**: `TECH` · **State**: OPEN

Concrete tools for clinical validation, publication of guides, static analysis of the interface descriptor, compatibility checking and analysis of legacy messages: **names, versions and invocation methods not verified**, and `PROTO` does not invent them. They must be fixed to exact version and censused in the inventory of third-party components, because a tool that validates regulatory artifacts is itself a component to be qualified. The five **requirements** it must satisfy are in [`docs/04_protocols/10-conformita-e-prove.md`](../04_protocols/10-conformita-e-prove.md) §6

### Q-134

**From**: `PROTO` · **To**: `COMP`, `SEC` · **State**: OPEN

Permitted signature envelope formats for a document destined for the health record, certificate requirements, time stamp requirements and discipline of the confirmation signature appended by the infrastructure: `PROTO` **does not reconstruct them from memory** and [`docs/04_protocols/03-documenti-clinici.md`](../04_protocols/03-documenti-clinici.md) §6.2 marks them `[NV]`. Precise regulatory investigation is needed before publication

### Q-135

**From**: `PROTO` · **To**: `ARCH` · **State**: **RESOLVED by `ARCH` - `PROTO`'s proposal accepted** ([`docs/adr/0025-formato-dei-token-verso-l-esterno.md`](../adr/0025-formato-dei-token-verso-l-esterno.md)): **opaque tokens to the outside, self-contained only within the perimeter**. The decisive reason is **actual revocation**: in a system that handles health data the question "since when has it been unable to access?" must have as answer an instant, not an interval, and the three situations that pose it - lost device, suspended integrator, revoked consent - are ordinary lifecycle events, not remote hypotheses. Four operational consequences added by this area: (1) the gateway is a **critical component** to be made redundant, with its unavailability treated as system unavailability and the latency cost **declared**, not hidden; (2) the resolution is stored for a **very brief duration**, sufficient to absorb calls from the same interaction but not to nullify revocation, with a maximum imposed; (3) **revocation invalidates the storage**, it does not await its expiration; (4) introspection on high-impact operations (`P-10`) **remains mandatory even with opaque tokens**: it is a second-level verification of the mandate state, not a substitute for revocation. Declared cost to integrators accustomed to inspectable format: it is friction to document **with the reason**, otherwise it is read as arbitrary limitation

**Format of tokens to the outside.** `PROTO` proposes tokens **opaque to the outside**, translated into self-contained tokens by the gateway: gives actual revocation, no claims exposed to interceptors, constant header size. The cost is that the gateway becomes a critical component to be made redundant, and latency changes. The self-contained alternative has a **revocation window** equal to the residual life of the token, mitigated in [`docs/04_protocols/08-identita-e-autorizzazione.md`](../04_protocols/08-identita-e-autorizzazione.md) §8.3 by short durations and by introspection on only irreversible operations (`P-10`). Impact on latency and topology: **decision by `ARCH`**

### Q-120

**From**: `FUNZ` · **To**: `ARCH`, `COMP` · **State**: **RESOLVED by `ARCH`** · **OPEN toward `COMP`** for the sole declared residual. Response from `ARCH` ([`docs/adr/0026-regole-del-piano-di-telemonitoraggio.md`](../adr/0026-regole-del-piano-di-telemonitoraggio.md)): **closed set of constructs**, each with declared semantics and its own proofs, extendable only with a release and explicit decision, never by configuration; the rule is part of the plan and is **versioned with it**; evaluation uses the version **in force at the instant of measurement** and operates on the instant of measurement, not receipt; the applied version is **recorded on the alarm** along with measurements and the threshold in force; determinism proved with **test vectors versioned** with the constructs; **no predefined threshold and no prefilling**; **no construct that deduces** thresholds from population or history, nor that interpolates or fills gaps in series. It is the set of properties this issue requested: expressive enough for threshold, trend, persistence, hysteresis, window and applicability, and closed enough not to be an arbitrary programming language. **Remains open the sole part addressed to `COMP`**: confirmation that the set of adopted constructs does not extend the frozen intended use. The ADR explicitly refers it to the compliance area, and it is the same issue recorded as `B-7-bis` in [`docs/02_architecture/09-decisioni-rinviate.md`](../02_architecture/09-decisioni-rinviate.md) §3

**Representation and execution of telemonitoring plan rules.** `RF-269` requires that measures be evaluated against the rules of the **individual plan in force at the instant of measurement**, with rule version recorded on the alarm (`RF-270`) and determinism verifiable with test vectors (`RNF-099`). Decision needed on **how rules are represented and executed**: the language must be expressive enough to cover threshold, trend, persistence, hysteresis, window and applicability conditions, and **restricted enough not to become an arbitrary programming language executed in production** - an engine too powerful is an attack surface and an object impossible to validate for regulatory purposes. It connects with **Q-142** (execution of imported clinical logic is declared out of scope: `OUT-24`) and with **Q-11** (licenses of scales and questionnaires). Decision by `ARCH`, with confirmation by `COMP` that the chosen form does not extend the frozen intended use

### Q-121

**From**: `FUNZ` · **To**: `PROD` · **State**: **RESOLVED by `PROD`** - Accepted without attenuation and set as **V-202**, making it a rule enforceable against every project material and not an editorial recommendation. The required prohibition is explicit: never commercial service level, never price list option, never declared coverage wider than that provided, formula of §11 non-negotiable. This area adds **an element not present in the issue**, which is its logical premise: **coverage is not a product characteristic, it is a characteristic of the subject providing it**. It follows the sole form of statement permitted in project material - *"within the coverage declared by the data controller, the service does X"* - and the consequence that the project cannot boast a coverage, not even as a default value: the product's coverage is the **ability to declare it, to make it visible with current state and to refuse activation of a plan that lacks it**. Follow coherence between public material and configuration as a **release verification**. There remains a residual not resolvable here and not resolvable in abstract: testing the formulation required by the issue with real users presupposes an interface, and the interface does not exist - it is a declared dependency toward the verification plan, not a gap

Residual from **Q-14**. Service hours coverage is now a verifiable requirement, but **its communication is product matter**. Approved formulation is needed and explicit prohibition to present it as commercial service level or as a price list option. The reference formula is in [`docs/03_functional/05-gestione-degli-allarmi.md`](../03_functional/05-gestione-degli-allarmi.md) §11 and **must not be watered down**: it declares that the service does not replace the emergency system, that outside the indicated hours data are not evaluated by a professional, and indicates the configured channel. It is a risk control measure of the weakest hierarchy level - informational - and precisely for this it must be written, tested with real users and made impossible not to see. **No material can declare coverage wider than that actually provided**

### Q-122

**From**: `FUNZ` · **To**: `INTEG`, `ARCH` · **State**: OPEN

**Minimum contract for the third-party measurement gateway** (`ATT-24`). Two elements are required by the security model and **it is not verified that market gateways expose them**: the **periodic presence signal independent of the measurement** (`RF-296`) and the **device status telemetry** - charge, connection, self-diagnostic, calibration (`RF-265`). `[NV]` on their effective availability, which `INTEG` can establish only by asking the gateway suppliers. The consequence is not of integration but **of safety**: without them the taxonomy of silence loses two technical categories out of seven, the residual silence becomes diluted, the number of empty contacts grows and with it operator fatigue. Definition of the minimum contract, strategy for gateways that do not meet it and declaration of the resulting degradation are needed

### Q-123

**From**: `FUNZ` · **To**: `COMP` · **State**: OPEN

**Delimitation of conversion of technical alarm into clinical alarm.** `RF-288` and `BR-138` establish that an unresolved technical alarm within the time defined in the plan generates a **clinical alarm of absence of surveillance**, with clinical recipient. This area considers it **detection of a fact** - the subject is not monitored - and not clinical interpretation, consistently with `V-09`. Confirmation from `COMP` is needed that the function falls within the frozen intended use and does not constitute production of new clinical information. It connects with **Q-01** and with the formulation of **V-144**

### Q-124

**From**: `FUNZ` · **To**: `ARCH`, `COMP` · **State**: **RESOLVED by `ARCH`** · **OPEN toward `COMP`** for the sole declared residual. Response from `ARCH` ([`docs/adr/0027-modalita-a-non-conservazione-del-contenuto-clinico.md`](../adr/0027-modalita-a-non-conservazione-del-contenuto-clinico.md)): the **history of alarms remains in the system** even in the non-retention mode, and is classified as documentation of an act, not as contributed clinical content. It retains threshold in force, version of the applied rule, reference to the measurements that produced it, deliveries, takeovers, forwards and outcomes. The non-obvious part of the decision, and it must be repeated here because it is what makes the response compatible with the obligation: **the history does not contain the values of the measurements, but their references** - if the measurements were contributed and removed, what remains is the fact that a rule was evaluated on an identified measurement and with what outcome, not the value. Indicators `RNF-095` and `RNF-096` thus remain calculable. **Remains open the sole part addressed to `COMP`**: confirmation of the taxonomy with respect to applicable obligations and determination of durations for categories not yet fixed, which the ADR explicitly refers to the compliance area

**Where the history of alarms lives in the mode without retention of clinical content.** `BR-174` adopts the obligation for which regional infrastructures do not retain generated data and documents; but the **alarm register** - threshold in force, applied rule, data that produced it, deliveries, takeovers, escalations, outcomes - is **documentation of an act** and without it is not reconstructible why an alarm did not trigger, nor are safety indicators `RNF-095` and `RNF-096` calculable. Decision needed on location, ownership and retention of this register in the two modes of operation. It connects with **Q-140** and with **Q-150**

### Q-125

**From**: `FUNZ` · **To**: `DOM`, `ARCH` · **State**: OPEN

**Coding of telemonitoring parameters and units of measurement.** `RF-256` requires explicit unit and declared conversion at the boundary, `BR-117` forbids presumed unit, `RF-255` requires origin. The system must establish with which coding system parameters and units are identified domestic, with what value constraint, and **what behavior the system keeps with the external terminological system disabled** (`V-03`: no main pathway can require it). The limiting case to resolve is the parameter present in a tenant's plan and not resolvable by the terminological gateway: rejection of ingestion or acquisition with unvalidated code and marking

### Q-126

**From**: `FUNZ` · **To**: `PROD`, `COMP` · **State**: **Part `PROD` RESOLVED, remains open toward `COMP` and toward the verification plan** - This area **does not coin the text**, for the reason the issue itself states: the text contributes to intended use, so its approval is not editorial and its validity depends on testing with representative users which today is not executable because the interface does not exist. What `PROD` fixes is the **contract that the formulation must satisfy**, so that the absence of text does not translate into divergent formulations: **(1) single source** - one sole set of strings versioned, referenced by all surfaces (patient interface, embeddable component, asynchronous channel, telephone fallback, manual entry, notifications, patient-facing documentation); no surface can coin its own, and divergence is detectable because the source is one. **(2) No attenuation permitted**: the text says that the service **is not** an emergency channel and indicates the channel to use; it does not say "might not be suitable", "we recommend", "in case of doubt". **(3) Three mandatory statements** are those of `BR-168` - expected response time, unsuitability for emergencies, alternative channel - and none can be omitted from a surface because "there's little space": if there's no space, the surface must be redesigned. **(4) Not themeable nor occludable**, by application of `V-163`. **(5) English translation is not free adaptation** (`D50`): the English formulation satisfies the same contract and divergence between the two is regulatory defect, not style. **Remains to `COMP`** approval of the text as material that contributes to intended use, and **to the verification plan** testing readability with representative users, which is a declared dependency and not a gap. It connects with `V-202`, which sets the same discipline on service hours coverage

**Approved formulation of declarations "not an emergency channel".** `BR-168` requires that the asynchronous channel with the patient, the telephone fallback and the manual entry interface persistently and non-closably declare expected response times and unsuitability for emergencies; `RF-320` and `RNF-105` set their verification. The text however **is material addressed to the patient and contributes to intended use**: approved formulation is needed, in Italian and English, verified for readability and tested with representative users. Risk if left undefined: each interface produces a different formulation and the weakest becomes the de facto standard

### Q-140

**From**: `DOM` · **To**: `ARCH` · **State**: **OPEN toward `ARCH`, reformulated on point (b) alone**, after points (a) and (c) were decided by `ARCH` with [`docs/adr/0027-modalita-a-non-conservazione-del-contenuto-clinico.md`](../adr/0027-modalita-a-non-conservazione-del-contenuto-clinico.md). Point **(a) decided**: the taxonomy by retention class, in eight categories that include the six regimes listed here - contributed clinical document, non-contributable clinical content, alarm history, consent and revocation evidence, access and operation register, access and authentication data, clinical parameter series, channel metrics - with the transversal rule that **absence of classification of a category fails startup**. Point **(c) decided**: the two modes coexist in the same distribution as configurations of the same code; the separate product branch was evaluated and discarded. **Remains open point (b), and the issue is reformulated on that**: the ADR adopts the rule that reading content no longer local passes through an assertion toward the external archive, but **does not designate which component resolves the reference** - clinical documentation context, anticorruption layer or outbound interoperability context. It is not an implementation detail: from that designation depend the point at which the authorization to re-read is applied and the declared behavior in case of external archive unavailability, which the ADR itself lists among the negative consequences to declare. **Open toward `ARCH`**

**Mode of operation without retention of clinical content.** DM 19 November 2025, art. 12 establishes that regional infrastructures **do not retain** generated data and documents: documents are contributed to the health record by healthcare facilities; authentication and access data last twelve months, registers twenty-four. The domain model foresees it as a mode (`DM-46`, [`docs/05_domain/04-documenti-clinici.md`](../05_domain/04-documenti-clinici.md) §9): after contribution remain identification, minimal metadata and proof of takeover, and reading always passes through an abstraction that resolves locally or toward the external repository. **An ADR is needed** on: (a) the taxonomy of data by retention class - clinical content, session recording, quality telemetry, device status telemetry, access register, authentication data: **six distinct regimes**; (b) the point at which the reference to a no-longer-local content is resolved; (c) the coexistence of the two modes in the same distribution. It connects with point 5 of `B1` § "Impact on design decisions"

### Q-141

**From**: `DOM` · **To**: `FUNZ` · **State**: **RESOLVED by `FUNZ`** - Identifiers assigned and frozen (V-120). One-to-one correspondence: **measurement wait** (`DM-57`) → `RF-293` window derived from plan, `RF-294` event generated by passage, `RF-295` distinction between unwanted and unreceived measurement, `RF-304` reconciliation on data return, `BR-150`..`BR-152`; **service hours coverage as service entity** (`DM-96`) → `RF-309` versioned data, `RF-310`..`RF-314`, `RNF-102`, `RNF-109`, `BR-160`, `BR-161`, plus the constraint position **V-122**; **feasibility evaluation with three independent outcomes** (`DM-95`) → `RF-344` three distinct dimensions, `RF-345` digital compliance as autonomous phase, `RF-343` appropriateness gate, `BR-180`..`BR-182`; **consultation scope as aggregate with expiration** (`DM-16`) → enabling relationship `CONSULT_SCOPE` already in force, with the complete use case `UC-12` and automatic revocation as error flow `E3`, rules `BR-014` and `BR-184`; **outcome distinct from state** (`V-141`) → fully adopted in [`docs/03_functional/03-casi-d-uso.md`](../03_functional/03-casi-d-uso.md) §1 and §4, where the outcome table assigns to each **who decides it** and **which administrative effect is permitted**, with `EX-NOSHOW` and `EX-TECH-PATIENT` kept separate even in effect (`BR-024`); **measurement source level** (`DM-54`) → `RF-255` mandatory and non-modifiable origin, `RF-257` detection conditions, `RF-258` declared reliability, `RF-253` distinction between self and hetero-administration, `BR-112`, `BR-113`. None of the six required reopening existing identifiers

**Six domain entities introduced by this area await requirement identifiers**, and none is covered by the existing `RF-*` catalog: **measurement wait** (`DM-57`, operational form of `V-09`), **service hours coverage declared as service entity** (`DM-96`), **feasibility evaluation with three independent outcomes** (`DM-95`), **consultation scope as aggregate with expiration** (`DM-16`), **contact outcome distinct from state** (`V-141`), **measurement source level** (`DM-54`). It connects with **Q-12**: four of the six uncovered areas listed there find their modeled form here. Identifiers must be assigned by `FUNZ` and **frozen** under `D45`

### Q-142

**From**: `DOM` · **To**: `COMP` · **State**: OPEN

**Confirm that local execution of clinical logic is out of scope.** DM 19 November 2025, Ann. 3 § 3.2 provides that the platform consume from the national glossary terminologies **and** guidelines, pathways and protocols "with logic expressed in a clinical expression language" (`REQ-57` of `B1`). This area distinguishes the two capabilities (`DM-86`) and designs so that **the executor of logic is absent by construction, not disabled by configuration**. Confirmation from `COMP` is needed that the distinction holds with respect to intended use, and indication of what the failure to consume those resources entails for attachment to a regional infrastructure. It connects with **Q-01**

### Q-143

**From**: `DOM` · **To**: `ARCH`, `INTEG` · **State**: **RESOLVED by `ARCH`** ([`docs/adr/0030-due-proiezioni-del-piano-da-una-sola-fonte.md`](../adr/0030-due-proiezioni-del-piano-da-una-sola-fonte.md)): generation lives **in the telemonitoring context**, which is the plan owner - not in the documentary context and not in the anticorruption layer: the documentary form is a **serialization of the canonical dataset** of the plan. The authoritative artifact is the **versioned declarative plan** with temporal validity; **both projections are generated and neither of the two is directly writable**; each construct of the language carries an **approved textual formulation**, versioned with the construct and translated; the document carries the **version of the plan** from which it was generated, which is what allows at distance to verify that the contributed document corresponds to the version then in force. On the second half of the question - how automatic correspondence between the two projections is verified - the answer is that **comparison between two artifacts is no longer the safeguard**: divergence is impossible by construction, because neither projection is drafted. The only channel through which non-generated content re-enters is the **free annotation** by the professional, which is permitted, adds to the generated text without replacing it and is marked as annotation

**The telemonitoring plan has two mandatory projections generated from the same source** (`DM-91`): an **executable** one, which the evaluation engine uses, and a **documentary** one, which goes to the health record as a dedicated type (lett. t) and which the format requires in **descriptive textual form** ("rules of behavior in violation of thresholds", Ann. 1 § 2.24). If drafted separately they diverge, and divergence between what the plan declares and what the system does is a patient safety defect. Decision needed on **where generation lives** and how automatic correspondence between the two projections is verified

### Q-144

**From**: `DOM` · **To**: → **ORCH** · **State**: **RESOLVED - D55.** The sponsor has **confirmed freezing** on the formulation "deferred collection of parameters for periodic professional review". Class IIa and software safety class B. The real-time formulation is **excluded**. The domain model was already written that way: no rewriting needed. It follows a permanent prohibition - no function can be added if it shifts the system toward clinical real time, and evaluation must be done **before** writing the function

**Freezing of the formulation of telemonitoring intended use.** The domain model is written entirely on the formulation **"deferred collection of parameters for periodic professional review"** (`V-144`), which under `D46` maintains Class IIa and software safety class B; the alternative formulation "real-time monitoring of vital parameters" brings into Class IIb and class C, with 12-18 months and an order of magnitude cost difference. `D46` requires that intended use be **frozen and submitted to external review before** engaging any Notified Body. **Decision by the sponsor**: confirm freezing on the first formulation and its propagation to documentation, interface and public communication. A subsequent change involves rewriting chapter 02 and chapter 05 of the domain area

### Q-145

**From**: `DOM` · **To**: → **ORCH** · **State**: OPEN

**Deliberate relinquishments of available technical capabilities.** The domain model relinquishes, by choice of scope and not by technical limit, to: **automatic facial detection** to ascertain the presence of third parties in session; **reliability weights** applied automatically to measurements based on origin; **calculation of risk scores, prognosis and interactions**; **interpolation or gap-filling** in time series; **calculation of clinical outcomes** and synthetic result indices; **deduction of thresholds** from population or history. Each relinquishment is motivated in chapters 02, 05 and 08 and contributes to maintaining the scope of `V2`. **Decision by the sponsor**: confirm the six relinquishments as product choices, adopt them in the declaration of intended use and in public communication, and submit them to change control - they are precisely the "one user story each" features from the reclassification of `D26`

### Q-146

**From**: `DOM` · **To**: `COMP` · **State**: OPEN

**Alignment with the National Telemedicine Glossary by AGENAS, v. 1.0.0 of 29 January 2026**, and with the Business Glossary of the national infrastructure. The glossary of the domain area ([`docs/05_domain/09-glossario-del-dominio.md`](../05_domain/09-glossario-del-dominio.md)) states for each entry if it coincides with the national glossary, if it specializes it or if it is proprietary to the project: **entry-by-entry alignment has not been executed** because the detailed content of the national glossary has not been read in this area. It must be performed **before freezing** chapter 09 and before English translation, because divergence discovered afterwards propagates on two languages

### Q-147

**From**: `DOM` · **To**: `FUNZ`, `PROD` · **State**: **Part `FUNZ` CONFIRMED, remains open toward `PROD`** - Confirmed and adopted: the service catalog bears **both** taxonomic attributes and neither is derivable from the other (`RF-030`, extended). The consequence on authorization is accepted without attenuation: **the professional constraint applies to the activity, not the minimum service**, and is already codified as a domain constraint not configurable by the tenant (`BR-011`, reiterated in the table "what a tenant cannot configure" in [`docs/03_functional/04-regole-di-business.md`](../03_functional/04-regole-di-business.md) §11). Authorizing on the minimum service would authorize too much: teleconsultation and teleconsultancy share the service and have different permitted actors, and teleconsultancy is permitted among different professions while teleconsultation is not. Additionally added the constraint that channel admissibility is verified **at booking**, not at delivery (`RF-041`, `RF-346`). **Remains to `PROD`** alignment of public material, which today tends to use only the minimum services taxonomy · **CLOSED by `PROD`** - Accepted and set as **V-201**: no public project material uses a single taxonomy, and both appear together with their **non-coincidence**, which is the information that matters. Applied in [`docs/00_overview/02-le-quattro-prestazioni.md`](../00_overview/02-le-quattro-prestazioni.md) §2, where the correspondence table explicitly shows the three points where the two taxonomies diverge - the unification of teleconsultation and teleconsultancy, the disappearance of telereferal as a standalone service, the promotion of telemonitoring from operational mode to minimum service - and §5.2, where the four differences between teleconsultation and teleconsultancy are listed one by one. **Two clarifications this area adds, both addressing material not yet written.** (a) The title "the four services" is permitted **only if accompanied by the explanation that under one of the four minimum services stand two distinct activities**; used alone it is the abbreviated form that generates the authorization error, and the chapter declares it in the opening of §2. (b) The residual this area **cannot** close is the **project public page**, which has not been rewritten and which today uses only the minimum services taxonomy: it is included in `Q-185`, open toward `ORCH`, and is not remedied by writing a documentation chapter

**The service catalog bears two taxonomic attributes, not one** (`DM-04`): the activity under Agreement 215/CSR 2020 and the minimum service under DM 21 September 2022, which **do not coincide** (§ 3.1-3.2 of chapter 01 of the domain area). Operational consequence to adopt: **the professional constraint applies to the activity, not the service** - teleconsultation and teleconsultancy stand in a single minimum service and have different permitted actors. Authorizing on the service means authorizing too much. Confirmation is needed that the functional catalog and the product material reflect both taxonomies and do not choose one

### Q-190

**From**: `GUIDA` · **To**: `TECH` · **State**: OPEN

**Names and form of local environment commands.** [`docs/10_fondamenti/17-ambiente-di-sviluppo.md`](../10_fondamenti/17-ambiente-di-sviluppo.md) takes a person from clone to functioning system but **does not invent names**: remain `[NV]`, and it falls to `TECH` to settle them once the executable skeleton exists: the prerequisite verification script, the selective startup groups of services in composition (database alone · database plus application service · complete set with media and federation), invocation of the migration tool, the command of the synthetic data generator with its size profiles, the aggregate command that executes **locally** the mandatory checks before the proposal, and the procedure for the local origin certificate needed for testing between two devices. They are the six names a contributor types on the first day

### Q-191

**From**: `GUIDA` · **To**: `TECH`, `ROAD` · **State**: OPEN

**Memory and disk consumption of the local environment: not measured.** Module 17 §1.4 publishes the **method** of calculation - services in container, platform virtual machine, interface chain, ephemeral test containers that run **in addition to** the environment already powered on - and **does not publish figures**, because none has been measured. A measurement on declared reference machine is needed and the declaration of the **minimum practicable profile**: it is the condition for an external contributor with modest machine to participate, which is precisely the objective of D35

### Q-192

**From**: `GUIDA` · **To**: `TECH`, `ARCH` · **State**: OPEN

**A single register of configuration parameters is missing.** Module 16 §9 establishes the four levels (`V-193`) but **the list of parameters does not exist in one place**: it is scattered between [`docs/02_architecture/05-multi-tenancy.md`](../02_architecture/05-multi-tenancy.md) §2.3, `08-viste-di-deployment.md` §§3.2 and 8, the technical area and the functional area. A contributor who reads module 16 and asks "which ones, and who sets them?" has no answer. **A generated artifact, not hand-drafted**, is needed that for each parameter declares: name, level of belonging, hard limit that validates it, behavior if value is absent, and whether its modification requires a restart. Two properties make it useful rather than decorative: **it fails the build** if a parameter exists in code and not in the register - otherwise it diverges at the first addition - and **it powers the blocking configuration checks on startup** already listed in `08-viste-di-deployment.md` §8, which today are a closed list written by hand. It connects with `V-193` and with the requirement of `ADR-0027` that absence of classification of a data category fails startup: it is the same mechanism applied to parameters

### Q-193

**From**: `GUIDA` · **To**: `PROTO`, `TECH` · **State**: OPEN

**Tools for validating clinical profiles executable locally.** Resumes Q-163 from the contributor side: without a reproducible command, the request of `CONTRIBUTING.md` to validate resources before opening the proposal **is not executable**. Requirements the solution must satisfy (module 17 §7.1): profiles **pinned by version as a build artifact** and not downloaded at the moment, because upstream change cannot change the outcome of already-executed validation; explicit distinction between conformance to the base model and conformance to the profile, which is the distinction that matters in practice; tool censused in the inventory of third-party components with exact version, because **a tool that validates regulatory artifacts is itself a component to be qualified**

### Q-194

**From**: `GUIDA` · **To**: `TECH`, `SEC`, `COMP` · **State**: OPEN

**Compatibility between synthetic identifiers and check G10.** The generator must produce identifiers that are together **syntactically valid** - to pass format validation throughout the pathway - and **recognizably synthetic**, so they cannot coincide with real persons' and not trigger G10 as false positive on every fixture. The proposed technique (module 03 §10.4, taken up in module 17 §5.4) is the **unassigned municipality code** in the cadastral code position, plus the intervals of temporary records and the V-192 marker. Confirmation is needed that **the G10 detection rule and the generation rule are designed together**: written separately, either the check blocks every fixture or blocks nothing

### Q-196

**From**: `GUIDA` · **To**: `SEC` · **State**: OPEN

**Ranges of addresses forbidden for relay node confinement testing**, even in the local environment. Module 17 §6.6 prescribes testing that, with valid credentials, **every** attempt to create permissions toward internal addresses fails - localhost, form mapped in IPv6, private addresses, public address of the node itself - and reiterates V-10: the list is defended in depth, the primary defense is network isolation on egress. The list of ranges and its maintenance remain with `SEC` as a residual of Q-16: **without the list the proof is not writable** and the constraint remains a declaration

### Q-195

**From**: `GUIDA` · **To**: → **ORCH**, all areas · **State**: OPEN

**A public and maintained list of first-entry contributions, by area, is missing.** [`docs/10_fondamenti/18-contribuire-per-area.md`](../10_fondamenti/18-contribuire-per-area.md) indicates for each of the ten areas "the first sensible contribution", but the indication remains **descriptive and not actionable** until an actual set of open issues exists, labeled and connected to those sections. It is the point at which, in an open source project, the difference between an acquired contributor and a lost one is measured in minutes. What is needed: the label, the criterion with which an area produces and maintains its own issues, and who verifies the list does not empty. No area can produce it alone, because the value lies in covering all areas

### Q-197

**From**: `GUIDA` · **To**: → **ORCH**, `PROD`, `COMP` · **State**: OPEN

**Who responds to reports that are not code, and within what time.** `18-contribuire-per-area.md` §14 declares that clinical review, verification of regulatory sources, translation, accessibility testing with actual assistive technologies and signaling of ambiguities **count as much as code and are scarcer**. This statement is sustainable only if those reports receive a **recorded outcome and a motivated response**, even when the outcome is rejection: a contributor who signals an ambiguity and receives no response does not signal a second time, and with them is lost the only source of that type of information. `GOVERNANCE.md` disciplines roles and decision process but **does not assign this safeguard**. It connects with `Q-174` for the part of public texts

### Q-198

**From**: `GUIDA` · **To**: `ORCH`, sito · **State**: OPEN

**Double naming of module 18 in references.** The module exists as [`docs/10_fondamenti/18-contribuire-per-area.md`](../10_fondamenti/18-contribuire-per-area.md); pre-existing references in `CONTRIBUTING.md` (Italian and English) and in `00-come-usare-questa-guida.md` pointed to a different name and **have been aligned**. One thing remains to do and it is not editorial: **insert the verification of reachability of references exiting `docs/` among the blocking checks before first deploy**, because check `G9` verifies internal links and not absolute ones toward the repository. It connects with `Q-26`, which poses the same problem for `NOT-A-MEDICAL-DEVICE.md`, `THIRD-PARTY-TERMINOLOGY.md` and `SECURITY.md`

### Q-199

**From**: `GUIDA` · **To**: `COMP`, `TECH` · **State**: OPEN

**Register of verifications of source validity.** [`docs/10_fondamenti/20-fonti-primarie.md`](../10_fondamenti/20-fonti-primarie.md) §11 establishes the method by which it is verified that a source is still in force and the rule that every time-dependent citation carries with it **the date at which it was verified**. Today that date is recorded unevenly: some entries carry it, most do not. Without a single form and periodic trigger the §11 remains a method without verifiable application, and the consequence is concrete - when a determination is replaced or a norm exits the list of harmonized ones, **it is not determinable which documents depend on it**. What is needed: the form of the registration, the place (in the document that cites, or in a generated artifact), and the trigger of re-verification. It connects to `V-195`

### Q-170

**From**: `COMP` · **To**: → **ORCH** · **State**: OPEN

**Form of publication of the intended use trace** (`MDR-IU-001`): integral, only structure without the text of substantial sections, or delivery on request to those who declare wanting to use it. It is not an editorial choice: art. 2, point 12, of the MDR derives intended use also from published material, so **a document titled "intended use" and published under the project name is precisely the type of material from which a third party can derive it**, contradicting `D49`. The three qualifications adopted - trace and not declaration, subject is the identified distribution, document without signature and approval - hold if the document is read and do not hold if it is cited. [`docs/08_compliance/02-qualificazione-e-classificazione.md`](../08_compliance/02-qualificazione-e-classificazione.md) §5

### Q-171

**From**: `COMP` · **To**: → **ORCH** · **State**: OPEN

Whether the project must also produce the **documentary package of Title III of the regulation on the European health data space** for electronic health record systems, or if it remains entirely the burden of whoever places it on the market. `[NV]` on the exact mapping of dates and definitions, to be verified by `COMP` on the text of the regulation. [`docs/08_compliance/01-inquadramento-normativo.md`](../08_compliance/01-inquadramento-normativo.md) §7

### Q-172

**From**: `COMP` · **To**: → **ORCH**, `ARCH` · **State**: OPEN

**Distribution identification versioning structure and its relationship with the base unique identifier.** The project does not assign the identifier - it is the manufacturer's act, item 1.1 b of Annex II - but its choice of **how a distribution is numbered and composed** determines what that identifier will identify, which modifications are new versions of the same device and which are a different device. It must be decided **before the first release of the distribution**: a subsequent renumbering is a discontinuity of identification, and identification discontinuity in a registered device is not remedied with a release note. It connects with `D17` and with the distribution policy. [`docs/08_compliance/04-fascicolo-tecnico.md`](../08_compliance/04-fascicolo-tecnico.md) §§4, 17

### Q-173

**From**: `COMP` · **To**: `DOM`, `FUNZ` · **State**: OPEN

Whether **presentation of measured parameters constitutes "measurement function"** under the MDR, with the metrological requirements of Annex I that would follow and with entire rows of the general requirements matrix that depend on it. The answer depends on a fact that this area does not know and must not deduce: **whether the system converts units, rounds or transforms received values, or presents them as received**. On it also depends the validity of an already-motivated exclusion in the quality management system. [`docs/08_compliance/02-qualificazione-e-classificazione.md`](../08_compliance/02-qualificazione-e-classificazione.md) §12; `04-fascicolo-tecnico.md` §9

### Q-174

**From**: `COMP` · **To**: `PROD`, `GUIDA` · **State**: **Part `PROD` ACCEPTED, remains open toward `GUIDA` and for the procedural part** - Accepted and set as **V-203**. This area adds three operational elements. **(1) Explicit perimeter of texts subject**: public page, `docs/00_overview/`, `README`, repository description and topics, announcements, release notes, responses to specifications. The perimeter must be written, because a safeguard without perimeter applies to what someone remembers to submit. **(2) The checklist is joint, not a single one**: to the forbidden formulas of `01-inquadramento-normativo.md` §11 are added those of [`docs/04_protocols/10-conformita-e-prove.md`](../04_protocols/10-conformita-e-prove.md) §2, which are technical rather than regulatory in nature - "conformant" on informational maps, "standard" on expired or never standardized drafts, "conformant to the Italian guide" without the version, "encrypted to the extremes" without the non-retention mode condition. Two separate lists produce two partial reviews. **(3) The rule this area has used on itself and which it proposes as the criterion of review**: **a summary cannot be more affirmative than the source it summarizes**. It is the concrete way a synopsis changes intended use - not by asserting the false, but by dropping a condition because it fit tight in a table row. In doubt the source is cited instead of rephrased. **Remains open** what this area does not decide: who executes the review, with what recording of outcome and with what cadence on already-published texts, which is quality system procedure and belongs to `COMP`; and the articulation with `GUIDA` on texts of `10_fondamenti/`

**Passage of regulatory review of public texts before publication**, with the table of forbidden formulas of [`docs/08_compliance/01-inquadramento-normativo.md`](../08_compliance/01-inquadramento-normativo.md) §11 as a checklist. It is the procedural corollary of `V-171` and the **only safeguard of the compliance area that no automatic verification intercepts**: a modification to the public page does not go through code review, does not appear in a release bill and does not fail any check - and it is, in this domain, the fastest way to change a product's intended use without touching a line

### Q-175

**From**: `COMP` · **To**: `PROD`, `TECH` · **State**: OPEN

**The device and the reference environment are part of the use specification, not a performance parameter.** Clause 5 of IEC 62366-1 requires that the use specification declare the **intended use environment**: until the mid-range device and reference network are declared, `UE-SPEC-001` **is not completable** and `RNF-106` is not verifiable. It re-raises `Q-115` with the consequence that that issue did not record: it is not a delay in performance measurement, it is a **technical file gap**, and as such is not filled with a value chosen afterwards - it must be declared, motivated and used as the basis for validation

### Q-176

**From**: `COMP` · **To**: → **ORCH** · **State**: OPEN

**UPDATED BY `D58` - the recipient changes, not the substance.** It is no longer "whether to produce a dossier reusable by a third party who will certify", but **whether to start now the dossier of the state of our clinical evaluation pathway**. It remains the only substantial part of clinical evaluation that **does not require the formal role of manufacturer** - it describes the reference clinical practice for services in scope, from public sources - and remains reusable by whoever certifies. **What changes is the weight of the decision**: the benefit is no longer a service rendered to a hypothetical third party but an advance on the project's critical path, and the deferral is a cost of ours. The cost remains real and must be declared: it requires a **documentable clinical expertise that the project today does not have internally**, so it is a commitment of external resources and not an extension of documentation. Decision by the sponsor

### Q-177

**From**: `COMP` · **To**: → **ORCH** · **State**: OPEN

**Whether the marked identified distribution is available only through a channel that identifies the recipient.** It does not touch the repository openness, which remains unchanged (`D51`): it concerns the **other artifact** of `D17`. From this decision depend two obligations otherwise not satisfiable: the **volume of sales and estimate of the user population** required by the periodic security update report (art. 86), and the **traceability of installed versions** required by Annex III. If the certified distribution were obtainable anonymously, two mandatory entries would remain empty and the gap **would not be remedied afterwards**. **UPDATED BY `D58` - the recipient changes, not the substance**: the two obligations no longer fall on a third-party manufacturer but on the **manufacturing subject to be established**, i.e. on us. The decision on the channel must therefore be made **before** the first identified distribution exists, because it is the point beyond which missing data are not recovered

### Q-178

**From**: `COMP` · **To**: `SEC`, → **ORCH** · **State**: OPEN

**How the project learns of an incident that occurred in a derived installation of which it is unaware.** Who takes the code, modifies it and puts it in service without any relationship with the project is not reachable by any of the envisioned measures, and it is the accepted consequence of open licensing. But **there is no input channel today**, and the absence is not neutral: a defect that the project does not know **remains in the code of whoever uses it**. Decision is needed on form, recipient and commitment of response of an input channel upstream, distinct from the output channel of coordinated vulnerability disclosure that operates downstream

### Q-179

**From**: `COMP` · **To**: → **ORCH** · **State**: OPEN

**PARTIALLY CLOSED BY `D57`; remains open on the form of the warning.** Attribution is decided: [`docs/08_compliance/09-percorso-e-calendario.md`](../08_compliance/09-percorso-e-calendario.md) no longer contains dates "of third parties" but **internal project planning** (`D57`), and the premise of the issue must be read in that form. **Remains open** the point that `D57` does not decide: the **form of publication** of the calendar and the formulation of the warning that accompanies it. The risk is unchanged and is not mitigated because the dates are ours - publishing a plan without a warning placed **above** and not below is the fastest way to produce the statement forbidden by `V-171`, i.e. that the product will be marked by a date. Consistent with `Q-170` (form of publication of the intended use trace) and `Q-174` (regulatory review of public texts)

### Q-270

**From**: `COMP` · **To**: `COMP` (chapters 06, 07, 08), → **ORCH** for assignment · **State**: **RESOLVED - reformulated `06`, `07`, `08`.** The phrase "who certifies" as a third-party actor **appears in no chapter of the area anymore**: verified with text search on ten chapters. Applied `V-270` in all four forms, **with the distinction deliberately preserved** - "the manufacturer" where the act is reserved to the role (approve the validation protocol, conduct the summative, sign `UEF-001` and `CE-REP-001`, be the holder of surveillance obligations), "the manufacturing entity, to be constituted" where the passage presupposes legal existence, "the project in the role of manufacturer" where the text refers to who intends to undertake the pathway, "the installer"/"the integrator" where the text referred to them. **Two prior attributions were improper and have been corrected**: in `06` §6.2 failure in accessibility was attributed to the manufacturer while it weighs on the installer for the declaration and on the project for the product properties; in `06` §9 the final column mixed manufacturer acts and installer acts. **No notice attenuated**: each chapter opens with the negative state of fact in first position and closes the gap of `V-271` with words proper to its object - "an accessible interface is not a validated interface" in `06`, "technical evidence is not clinical evidence" in `07`, "no one supervises today's installations" in `08`. No date and no window: the only occurrence of "within" in `06` and `07` is inside the statement of the prohibition; in `08` remain the regulatory terms of the surveillance regime, which are legal obligations and not project dates, and the opening notice declares it. Placed `V-273` and `V-274`; opened `Q-273`…`Q-276` **REOPENED on 26 August 2026: the census had omitted chapter `05`.** The issue declared that "who certifies" as a third-party actor appears in no chapter of the area anymore. The verification contradicts it: `05-gestione-del-rischio.md` §1 states "the project is not a manufacturer" and carries the column heading "Who certifies" on nine allocation rows, and lacks the opening notice that `03`, `04`, `06`, `07` and `08` received. **The search that concluded otherwise was case-sensitive.** It is the most serious residue of the area, because `00-indice.md` §1.1 declares that the phrase is no longer used: until `05` remains as it is, **the index of the area says the false about itself**. Reassigned to `COMP` as step 1 of sequence `D63`.

**Residues of the third-party figure outside the redefined perimeter.** The reformulation mandated by `D57` and `D58` covered `00`, `01`, `02`, `03`, `04`; the phrase "who certifies" as a third-party actor **persisted in three unassigned chapters**: `06-usabilita-e-accessibilita.md` (frontmatter, §7 "the division between the project and who certifies", §9 title), `07-valutazione-clinica.md` (frontmatter, opening notice, row `Q-176` of the final table), `08-sorveglianza-post-commercializzazione.md` (§ with the column "Obligation of the project toward who certifies"). Until they are reformulated, **the area is internally incoherent** and a reader entering from one of those chapters derives the figure that `D58` eliminated. The register to apply is `V-270`; the rewriting model is `00` §1.1 and `02` §5.2. **No reformulation may attenuate the notices**: the acts reserved to the role remain reserved and the state of fact of the product is unchanged

### Q-271

**From**: `COMP` · **To**: `ROAD`, → **ORCH** · **State**: OPEN

**Residues and a editorial defect in `09-percorso-e-calendario.md`, chapter already reformulated.** Detected while reading it as register reference: (a) §4, "Holder" row of the table of the two milestones, says **"A manufacturer, which today does not exist"** - true as to the actor, but still formulated as a third party and not as "the manufacturing entity, to be constituted"; (b) §5, closure: the activities of the first thirty days **"weigh on who intends to certify", and "the project documents them as operational manual without assuming them"** - contradicts `D58`, which attributes them to us; (c) §6, "that who will want to certify **can do so**"; (d) §7.2, the statement of `V-179` contains a **defective repetition** - "enter the quality management system **of a quality management system**" - and closes with "the entry of an **extraneous** procedure". These are corrections of register and typo, **not of substance**: none touches the technical division or the notices. I have not applied them because the chapter is not in my perimeter

### Q-272

**From**: `COMP` · **To**: → **ORCH** · **State**: OPEN

**The constitution of the manufacturing entity is now an internal prerequisite with its own time, and no area is its holder.** `D58` attributes it to the project and `D57` places it in the internal planning; `09` §3 puts it on calendar. But it is an **act corporate and administrative**, not a documentation activity: legal form, seat in the Union, insurance coverage (art. 10, par. 16, MDR `[NV]`), identification of the person responsible for compliance with the regulations with a qualification dossier (art. 15). None of these is producible by a documentation agent, and until it is launched **every item of §12 of `04` remains blocked upstream**. The client must declare who conducts it and with what resources. Connects with `Q-181` and with the division left open by `Q-189`

### Q-273

**From**: `COMP` · **To**: `PROD`, → **ORCH** · **State**: OPEN

**Formative usability evaluations are now an activity of ours and not deferrable, but are not producible by a single person.** With `D58` the formative cease to be the contribution to an extraneous pathway and become the only assurance against the scenario that adds a quarter to the pathway (`06` §8); they are also the only item of the chapter that **awaits nothing** - they are conducted on prototypes, before the manufacturing entity is constituted, without a notified body and without a frozen interface. But observing a representative user while performing a task **requires subjects distinct from who designed the interface**, exactly like the internal audit and the release review: it is the limit of `D54`, and **it is not a matter of hours**. It must be established whether the function is acquired externally or whether the absence of formative is accepted as a declared risk, knowing it is the costliest risk in the entire chapter. [`docs/08_compliance/06-usabilita-e-accessibilita.md`](../08_compliance/06-usabilita-e-accessibilita.md) §§8, 10

### Q-274

**From**: `COMP` · **To**: → **ORCH** · **State**: OPEN

**Reformulation residue outside my perimeter: `Q-144` is closed by `D55`, but still appears listed among the open issues in `02` §12 and in `09` §10.** `D55` froze the intended use of telemonitoring on the formulation "deferred collection of parameters for the professional's periodic review", maintaining Class IIa and software safety class B. It is not a divergence of merit, but a reader entering from those two chapters derives a **blocking dependency that no longer exists** - and in `09` §1 the intended use is the node from which four branches depend, so the effect is amplified. In `07` §2.2 the section has been rewritten accordingly and the residue is declared there. Correction of register, not of substance; I have not applied it because the two chapters are not in my perimeter

### Q-275

**From**: `COMP` · **To**: → **ORCH** · **State**: OPEN

**The external review of the frozen intended use has not been conducted, and it is the only of the two conditions of `D46` that can be satisfied now.** `D46` and `D55` mandate it **before** engaging any notified body. Engagement presupposes the manufacturing entity to be constituted (`Q-272`); **the review does not**: it is commissioned to an independent regulatory reviewer without any legal entity existing. Deferring it exposes to the worst available risk - that the freezing holds until the first external confrontation and no further, when the clinical evaluation plan, the literature search perimeter, the hazard analysis and the use specification have already been written on top of it and all must be redone. [`docs/08_compliance/07-valutazione-clinica.md`](../08_compliance/07-valutazione-clinica.md) §2.2

### Q-276

**From**: `COMP` · **To**: `TECH`, `ARCH`, → **ORCH** · **State**: OPEN

**Two product capabilities of surveillance are retroactively unrecoverable like the four activities of `D45`, and do not appear in that list.** They are those of `V-178`: **stable taxonomy of counted events** and **conservation of diagnostics equal to the surveillance window**. Both **count events**: they do not turn on retrospectively, and the missing historical series is not reconstructed - consolidating or renaming a category makes **the statistically significant increase of art. 88 disappear** instead of detecting it. While the surveillance obligation was a third party's, the gap was their problem; with `D58` the holder will be us and the gap is **ours**. It must be assessed whether the two capabilities should be added to the list of retroactively unrecoverable activities of [`docs/08_compliance/09-percorso-e-calendario.md`](../08_compliance/09-percorso-e-calendario.md) §5, which today counts four. [`docs/08_compliance/08-sorveglianza-post-commercializzazione.md`](../08_compliance/08-sorveglianza-post-commercializzazione.md) §§5.2, 8.4, 10

### Q-180

**From**: `ROAD` · **To**: → **ORCH** · **State**: **RESOLVED - D53.** The client chose **the first installable release on 30 November 2026**, after the orchestration had exposed the tension and recommended the alternative. The decision is made. It follows an obligation for `ROAD`: **reduce the scope** to what a single contributor working part-time can build and verify in three months, declaring what enters, what is cut and which cuts are reversible

**Reformulation of the 30 November 2026 milestone.** As of 25 August 2026 there is no line of application code, and `D52` places **before the software** the completion of all documentation, full translation (`D50`), site construction, publication and verification of real functioning. Three options argued with the cost of each in [`docs/09_roadmap/02-traguardi.md`](../09_roadmap/02-traguardi.md) §4.2; **option 1 - "foundations and documentation", with the first installable release as an autonomous 2027 milestone - is the one recommended and the one the area applies in the absence of contrary decision**. Not separable from Q-181 and Q-182

### Q-181

**From**: `ROAD` · **To**: → **ORCH** · **State**: **RESOLVED - D54.** Declared capacity: **single contributor, part-time**. It is no longer a hypothesis. The division of `Q-189` remains open: which distinct-role registrations are accepted as declared risk and which are covered by acquiring the function externally

**Declaration of capacity**: how many people, with what skills, with what continuity. It is the missing variable without which **no internal date is calculable**; the entire roadmap is constructed under the declared hypothesis `H2` of `01-principi-e-metodo.md` §11. Point of irreversible decision declared: **15 September 2026**, beyond which the option that keeps `D5` unchanged decays on its own, because the milestones of class `A` and `C` occupy the first weeks anyway

### Q-182

**From**: `ROAD` · **To**: → **ORCH** · **State**: **RESOLVED - D56.** **Area-by-area assisted translation**, with automatic divergence control between Italian and English. Amendment to `D52`: full translation is no longer a prerequisite to every line of code and proceeds in parallel. Public notices, the foundations guide and the compliance and security areas remain non-negotiable prerequisites

**Production model of the full English version** (`D50`) and who executes it. Determines the milestone `T-06` (full English version) and, by dependency, `T-07` (bilingual site published and verified). Point of irreversible decision: **30 September 2026**; beyond that date the two milestones do not fall in 2026 and **no area can be declared closed pursuant to `D52`**

### Q-183

**From**: `ROAD` · **To**: `COMP` · **State**: OPEN

Which evidence of the **regulatory package** is deliverable on 30 November 2026 and which is not. Determines the perimeter of milestone `T-12`. Connects with Q-25

### Q-184

**From**: `ROAD` · **To**: `SEC`, `ARCH` · **State**: OPEN

**Temporal coordination on expected service levels.** This area requests that the **measurement capacity** - historicization of availability per tenant and per service, with granularity sufficient to recognize the exceeding of a threshold on the order of a percentage point on a daily basis - be a **requirement of the first release**, and that the **values** be determined before the first exercise with a real tenant. Adding the measurement afterwards means not having historical series in the moment they are needed. Residue of Q-152 and of B-6

### Q-185

**From**: `ROAD` · **To**: `PROD`, → **ORCH** · **State**: OPEN

**The public page does not appear aligned with the reformulations approved with `D19` and `D29`.** Every day of exposure is a day of unsustainable affirmation, and **a period of publication does not cancel**: the impact is unrecoverable, not deferrable. Aggravating to know: the intended use is determined **also by the promotional material**, so an unaligned statement **modifies the intended use**. A date is needed. Connects with Q-174 of `COMP`

### Q-186

**From**: `ROAD` · **To**: `COMP`, `TECH` · **State**: OPEN

**Declared support period** (residue of C-4 and of Q-155). Without the duration, the version dismissal plan **is not publishable** and the number of major versions to maintain concurrently is not determinable. It is not a tail item: it is a **prerequisite of the first distribution**

### Q-187

**From**: `ROAD` · **To**: → **ORCH** · **State**: OPEN

**Allowed concentration on a single integrator.** To what degree the dependency is acceptable, and at what moment the project procures a **second proof counterparty** - second real integrator, entity, or reference implementation built for that purpose. The constraint `V-188` presides over the form of the proofs, **not over the drift of priorities**: the drift is accomplished in five steps each of which is locally reasonable, and is observable only in review. Structural description in [`docs/09_roadmap/05-rischi-e-dipendenze.md`](../09_roadmap/05-rischi-e-dipendenze.md) §4, never by name (`R0`)

### Q-188

**From**: `ROAD` · **To**: `COMP`, → **ORCH** · **State**: OPEN

**Declared reserve for non-estimable items.** The research on the certification pathway classifies as non-estimable the response cycles to non-conformities, rework after summative validation and waiting time before being accepted by a body, and mandates putting them in the budget **as a declared reserve instead of omitting them**. Some items fall on the project **even without certifying** - third-party component surveillance, remedy windows, modification evaluation: it must be established who budgets them and in what form

### Q-189

**From**: `ROAD` · **To**: → **ORCH** · **State**: **OPEN - decision point 30 September 2026.** Updated after `D54`: it is no longer a conditional hypothesis ("if the project remains a single contributor") but the consequence of a declared capacity. Predefined outcome in the absence of decision: **declared gap on `RU-1`**, which remains dated forever because a review of an already-executed release is not a review. Registered as risk `R-29` (Ascertained, I5) and as irreversible cut `TG-20` in [`docs/09_roadmap/03-primo-rilascio-utilizzabile.md`](../09_roadmap/03-primo-rilascio-utilizzabile.md) §5.3; return in queue as `OL-22`, first position. **First concrete record, 27 August 2026**: the approval of the document control procedure was given by the person who drafted it. It is the predefined outcome applied for the first time to a real act, and the form chosen is the one this question prescribes - **the coincidence is declared in the act itself**, not withheld, so that the record remains true instead of appearing conformant. It does not close the question: it documents it, and the split between accepted risk and externally acquired function remains to be established

**Continuity of contributors and registrations in distinct roles.** If the project remains a single contributor, some registrations required by the quality management system - internal audit, release review, configuration verification executed by who did not write it - **are not producible internally**, because they require distinct subjects. It must be established which subset of the risk is formally accepted and which is covered by acquiring the function externally

### Q-280

**From**: `ROAD` · **To**: → **ORCH** · **State**: OPEN

**Constitution of the manufacturing entity and appointment of the person responsible for compliance with regulations** (`D58`). The decision on legal form, start of the constitution procedure, required profile and methods for finding the responsible figure is needed - including the admissibility of an external figure in the regime of micro and small enterprises. **It is class `B`: costs few hours and many months**, because it depends on administrative procedures and the availability of a documentable qualification, and **every week of delay is transferred in full to the end of the chain** without any increase in capacity recovering it. More steps of the internal planning formally presuppose it: signing a contract with a notified body requires a subject, the appointment requires a subject that appoints, the registration of the economic operator requires a subject to be registered. **The request for information to the bodies does not require the constituted subject and is sent before** (`T-14` criterion 3), because the queue is the actual constraint. Decision point: **19 September 2026**. Risk `R-30`; milestone `T-14` in [`docs/09_roadmap/02-traguardi.md`](../09_roadmap/02-traguardi.md) §3

### Q-281

**From**: `ROAD` · **To**: `PROD`, → **ORCH** · **State**: OPEN

**The difference between what the public material declares and what `RU-1` delivers.** Under `D53` and `D54` the first release comes out with a reduced and declared perimeter ([`docs/09_roadmap/03-primo-rilascio-utilizzabile.md`](../09_roadmap/03-primo-rilascio-utilizzabile.md) §5): it does not contain reporting, interoperability facade or embeddable component. On release publication the distance between the public material and the artifact **becomes verifiable by anyone**, and the intended use is determined **also by the promotional material**. The approved formulation declaring the difference is needed, with the same prominence as the function list. **Connects with `Q-185` and `Q-174`**, and with the additional notice that `D58` introduces: the intention to certify **is never to be formulated as a dated outcome** (`V-171`, `V-280`). Decision point: **31 October 2026**. Risk `R-19`

### Q-282

**From**: `ROAD` · **To**: `COMP` · **State**: OPEN

**Form of the declaration of the dated gaps in the technical file.** Four registrations are **dated to a period and not reconstructible afterwards**: release review, internal audit and independent configuration verification (`TG-20`); external review of critical safety code (`TG-21`); formative usability evaluation during development (`TG-22`); periodic anchored anchorization of the immutable log (`TG-23`). It must be established **in what form their absence is to be declared in the technical file** so it is recognized as a declared gap and not as an omission - and it is the form that who verifies expects to find. **`Q-189` decides the division, this issue decides the form**: they are complementary and neither substitutes for the other. Blocking criterion 17 of [`docs/09_roadmap/03-primo-rilascio-utilizzabile.md`](../09_roadmap/03-primo-rilascio-utilizzabile.md) §8; criterion 2 of `T-10`

### Q-283

**From**: `COMP` · **To**: `TECH` · **State**: RESOLVED

**Protection of main branch - RESOLVED on 26 August 2026.** Configured with `gh api -X PUT repos/fedcal/Telemedic/branches/main/protection --input -` and verified the same day: `gh api repos/fedcal/Telemedic/branches/main/protection` returns status `required_pull_request_reviews` active. A change proposal to a controlled document cannot now be merged without favorable review from the reviewer named for that category. **Form declared by the client**: the repository owner (client) can approve their own changes and merge them; contributors receive review from a named reviewer. This form **does not resolve the gap of `Q-189`** - under `D54`, the author approves themselves - but **makes it visible and circumscribed**: the system records it and the trace is attestable in the technical file as a declared gap

### Q-284

**From**: `COMP` · **To**: `TECH` · **State**: OPEN

**Cryptographic signature of commits - adopted on 26 August 2026, mandatory signature on pending branch.** The same procedure (§8) mandates commits with verifiable signature. **Adopted status**: local configuration `user.signingkey 13EEEA8DBE18B284`, `gpg.format openpgp`, `commit.gpgsign true`, `tag.gpgsign true`. Key `ed25519`, addressed to `Federico Calò <fedcal01@gmail.com>`, expiration 20 February 2029. **Remaining open step**: mandatory signature on the branch with precise trigger - until the public key is uploaded to GitHub, signed commits would appear "unverified" and the push would be rejected. It is the last step and has a specific trigger, not a date. Distinguish from `Signed-off-by` (DCO), which is **active and mandatory** and satisfies today ISO 13485 §7.5.8: cryptographic signature is a further and stronger control <!-- dato-reale-consentito: intestazione della chiave di firma, tracciabilità nominativa richiesta dalla clausola 7.5.8 di ISO 13485 -->

### Q-200

**From**: `PROD` · **To**: ORCH, `ROAD`, `COMP`, `SEC`, `INTEG` · **State**: **OPEN PARTIALLY SUPERSEDED BY FACTS on 26 August 2026, sequence delivered.** The premise no longer holds: `V-06` already carried the heading "AMENDED BY `D58`", `V-180` is already reformulated by `V-280`, and the three public notices are already rewritten on `D58` - verified on the text, not presumed. Letter (a) is satisfied. **The relation between the two fronts has however inverted, and it is the new fact**: public notices, overview, compliance and roadmap carry the new attribution; `03_functional`, `07_integration`, `04_protocols`, `06_security`, `01_technical` and the foundations guide still carry the one superseded. The two attributions **already coexist, in the worst direction**: the most visible document is updated, the one a reviewer opens to verify it is not. Letter (b) is delivered in `.telemedic/piani/D63-recepimento-del-ruolo-di-fabbricante.md` - five indivisible steps, each in both languages. Letter (c) is confirmed. **Remains open** until step 5 is closed.

**`D57` and `D58` are not yet received in the constraints in force or in the areas, and the overview is the first document written after them.** Status ascertained: **`V-06`** still states "the project does not apply CE marking, the regulatory material is a manual for who certifies" and **`V-180`** lists CE marking among the "milestones that are not the project's" - both formulations **amended by `D58` and `D57`** in the attributive part. The indexes of `08_compliance`, `07_integration` and `09_roadmap`, and the notices `NOT-A-MEDICAL-DEVICE.md`, `DISTRIBUTION-POLICY.md` and the `README.md` call, are built on the manufacturer figure as a third party. **`docs/00_overview/` is written on `D53`-`D58`** and declares the divergence instead of hiding it (alignment note in `00-indice.md` §3.5 and `01-visione-e-obiettivi.md` §5.1). Needed: **(a)** reformulation of `V-06` and `V-180` by their respective holders - not abrogation: the technical substance remains, the recipient changes; **(b)** the sequence of rewriting of the areas, because partial rewriting produces two coexisting attributions, which is worse than an outdated attribution; **(c)** confirmation that the admitted formula for marking is *"the project intends to assume the role of manufacturer; the entity is to be constituted; today the product does not carry marking; no date is declared"* and that **no abbreviated form of it is admitted**, because every abbreviation falls on the side of the intention and loses the state of fact

### Q-201

**From**: `PROD` · **To**: `GUIDA`, `ROAD` · **State**: OPEN

**Four modules of the foundations guide are cited and do not exist.** Missing are the `16` (project architecture), `18` (area → required knowledge matrix), `19` (glossary) and `20` (primary sources). It is not a gap symmetric to others: the guide is **a declared prerequisite of `CONTRIBUTING.md`** and `D56` places it among the three non-negotiable prerequisites of translation, so the four modules are on the critical path of two distinct commitments. Two weigh more than the others: the **glossary**, because it is cited by more areas and because its alignment to the national glossary must be accomplished **before** translation - a divergence discovered afterwards propagates to two languages (`Q-146`) -; and the **18**, because it is the document that tells a contributor what they must know before touching an area, and its absence makes the guide not actionable precisely where it is needed. The overview cites them as part of the guide without flagging their absence in the reading paths: if their production slips, **the references must be marked as not yet available**, because a reference to a non-existent document is worse than the absence of the reference

### Q-202

**From**: `PROD` · **To**: `ROAD` · **State**: OPEN

**The photograph of the state of fact now exists in two places and the two are already divergent.** [`docs/09_roadmap/00-indice.md`](../09_roadmap/00-indice.md) §4 reports as absent the overview and the compliance area and as twenty-four the entries of the decisions register; [`docs/00_overview/04-stato-e-limiti.md`](../00_overview/04-stato-e-limiti.md) §2 reports both complete and thirty entries. The two do not contradict - they are dated differently - but **a photograph without date is a photograph that ages in silence**, and it is the form of error that this area has imposed on itself not to produce. Three options, with different cost: **(a)** single place in `ROAD`, with the overview referring and not reporting - cost: the reader of the overview to evaluate must jump to another area precisely on the data they need first; **(b)** single place in `00_overview`, with `ROAD` referring - cost: the roadmap loses the basis of its own §4, on which it builds the two readings that determine the plan; **(c)** two places with **generated source**, i.e., a count produced by the verification chain and not written by hand. **Recommendation of `PROD`: (c)**, for the same reason that the bill of materials is generated and not compiled - an inventory written by hand lies within three months, and this has already begun. Decision and placement to `ROAD`

### Q-285

**From**: `TECH` · **To**: `COMP`, `PROTO` · **State**: OPEN

**List of admission of terminologies, which `T-03` does not produce and presupposes.** Control `G3` blocks on the content of coding systems with vincolated license, but to distinguish what is admitted from what is not, a **versioned list of what the repository can contain is needed** - which systems, in what form, with what depth. Today the script `scripts/verifica-terminologie.sh` incorporates that knowledge without it being declared elsewhere: it is a rule that lives only inside an executable, and changes when someone modifies the executable. **It is class `C`: it stands on the critical path of `T-03` and its traversal time does not depend on who executes the milestone.** Posted on 26 August 2026 because the wait flows in parallel to the work instead of at the end of the sequence. Work `L-03` of `.telemedic/piani/T-03-catena-di-costruzione.md`

### Q-286

**From**: `TECH` · **To**: `PROD`, `SEC` · **State**: OPEN

**List of forbidden terms of rule `R0`, which `T-03` does not produce and presupposes.** Control `G11` is defined in [`docs/01_technical/09-integrazione-continua-e-rilascio.md`](../01_technical/09-integrazione-continua-e-rilascio.md) §3 as "the automated translation of rule `R0`: a versioned list of forbidden terms, with update procedure". The list does not exist. Without it `G11` is not implementable, and rule `R0` remains presided over only by human review - which the document itself declares insufficient for the case in which a name ends up in a comment or in an example configuration file. It must be established **who compiles it, where it lives and with what procedure it is updated**; and it must be written with the caution that its object imposes, because a list of forbidden terms is itself an enumeration of those terms. Work `L-03`

### Q-287

**From**: `TECH` · **To**: `SEC`, `COMP`, → **ORCH** · **State**: **RESOLVED on 26 August 2026 - ephemeral identity.** The client chose the second family: no private key to protect, identity linked to chain execution, proof in a public transparency registry. **The decisive reason is `D54`**: custody of a key is a *recurring* burden - protection, rotation, revocation, continuity among contributors - and the declared capacity is of a single contributor part-time. A recurring burden that no one has the time to exercise is not a security measure: it is an abandoned key, which is worse than no key. **The accepted cost, declared and not attenuated**: the verifiability of artifacts depends on a third-party transparency registry and its availability *over time*, which for a medical device is a long horizon - technical documentation must be preserved for years after the last exemplar is placed on the market. It must therefore be written in the verification procedure by who installs (criterion 8 of `T-03`) **what to do if that registry is no longer reachable**, and must be reexamined when the manufacturing entity is constituted, because `D63` changes the conservation horizon. Determines the work of phase 4 of `.telemedic/piani/T-03-catena-di-costruzione.md`

**Custody of the signing material of artifacts and form of attestation of provenance.** Criteria 7 and 8 of `T-03` require signed artifacts and a verification executable by who installs. Both presuppose a decision that `T-03` cannot make: **where the private key lives and who controls it**. Two families of solution, with different consequences and neither preferable a priori - **signing with persistent key in declared custody**, which exposes a key to protect and rotate but does not depend on an external service; **signing with ephemeral identity linked to chain execution**, which eliminates the key to protect but ties verifiability to a third-party transparency registry and its availability over time. The choice also determines the form of attestation of provenance and what who installs must have to verify it. Under `D54` one criterion weighs more: the second family moves a recurring custody burden out of the project, and the declared capacity is scarce. **To decide before phase 4 of `T-03`**, not after: the choice changes the work, not just their configuration. Work `L-04`

### Q-288

**From**: `TECH` · **To**: `SEC`, → **ORCH** · **State**: OPEN

**Two pipeline controls cannot have a negative proof in the form that the rule imposes, and the rule does not provide for the exception.** `pipeline/README.md` rule 3 requires that each row of `pipeline/collocazione-dei-controlli.tsv` cite a case of the bench that saw that control fail. Two rows cannot satisfy it today, for different reasons and neither attributable to negligence. **`G1`, secret search**: lives as a third-party GitHub action, not as a local script; it is not installed on this machine and does not accept being pointed at a holding. Writing in the bench a substitute that imitates its behavior **would prove the substitute, not the control** - and would be the same species of lie that the table exists to prevent. **`G5`, bill of materials**: generates the bill and loads it as an artifact, but does not compare or assert anything; **there is no behavior today that can fail**, so there is nothing to prove until criterion 5 of `T-03` is implemented. Three ways, with different costs: **(a)** make `gitleaks` executable locally - versioned binary or container - and accept the burden of updating it; **(b)** admit into the format a declared status of *non-testable* with reason and review date, along the lines of `bloccante_dal`, with the known risk that an exemption box fills up for convenience; **(c)** leave the table in error, which is honest but makes a gate red for a debt that no one can close today, and a gate always red stops being read. Posted on 26 August 2026, work `L-02`. **Third instance, added the same day**: the gate on site construction - criterion 1 of `T-07`, half "links" of `G9` - has been tested in reverse by hand, with verified outcome, but **is not reproducible in the bench because building the site takes minutes** and the bench runs in seconds. The three instances have different causes and the same form: `G1` is not executable locally, `G5` had no behavior that could fail, `T-07/1` has a proof that exists but costs too much to be repeated. **The question that unites them**: the rule requires a case in the bench, or does it require a reproducible proof - and if the second, is a slow bench with separate execution an answer or the first step toward a bench that no one executes?

### Q-289

**From**: `TECH` · **To**: `PROTO`, `COMP` · **State**: OPEN

**The admitted conformity formulas are a rule written without any control, and the table declared the opposite.** [`docs/04_protocols/10-conformita-e-prove.md`](../04_protocols/10-conformita-e-prove.md) §2 lists the formulas that the project does not admit - "is compliant with" and similar - without proof that can fail. The row `T01-C8` of `pipeline/collocazione-dei-controlli.tsv` enumerated them among what `scripts/verifica-conformita-redazionale.sh` presides over: **the script does not search for them**, and no other control of the repository searches for them. The script implements three different things - unquoted frontmatter, relative references outside `docs/`, secret placeholders. The row has been corrected because it declared the truth; the debt remains intact. It is the same form of the two facts that the 26 August review register measures for the first time: **a rule written and not presided over by a control is not a rule**. It must be decided who writes the control and with what list of formulas, keeping in mind that the list is itself the catalog of what must not be written. Posted on 26 August 2026

### Q-290

**From**: → **ORCH** · **To**: `COMP`, `SEC`, `PROD`, `ROAD` · **State**: OPEN

**`D63` - the client decided on 26 August 2026 that the system must be suitable for delivering services to real patients.** It is not a scope expansion: it is the transition from "draft for who will certify" to "product that will be certified", and it is the operational implementation of `D58`. Immediate and non-negotiable consequences. **(a)** "Suitable for real patients" has one sole legal meaning: CE marking at the conclusion of a completed conformity evaluation. §3 of `08_compliance/02` has already determined that under MDR Rule 11 **Class I does not appear in any cell**: the minimum result is **IIa with notified body**, and no convenient intermediate box exists. **(b)** The realistic calendar is already written in `08_compliance/09`: certificates **June 2028**, marking and registration **July-August 2028**; the tension scenario brings certificates to December 2027 and is declared at **low probability**, "tension objective, not plan". **`RU-1` of 30 November 2026 remains an engineering milestone and remains not usable on patients**: the two things are not in conflict, they are on two different calendars, and confusing them would produce exactly the misleading statement that art. 7 of the MDR forbids. **(c)** `V-171` and `V-280` **remain in force**: the decision authorizes pursuing the marking, **not** declaring a date when it will be applied. **(d)** To be resolved immediately, because everything else rests on it: the **form of the legal entity** and the moment of its constitution. Until it exists, the manufacturer would be a natural person, and it is precisely the outcome the client wants to avoid. See `Q-291`

### Q-291

**From**: → **ORCH** · **To**: → **`COMP`** · **State**: OPEN

**Division of responsibility: what is achievable and what is not.** The client asked how to avoid appearing as the directly interested party in case of errors. The answer has two halves and must be kept together. **Achievable**: company assuming the role of manufacturer, so the responsible person is not a natural person; insurance coverage, which **art. 10, par. 16 of the MDR** makes an obligation and not a choice, "proportionate to the risk class, the type of device and the size of the enterprise"; person responsible for compliance with regulations **external**, which **art. 15, par. 2** allows to micro and small enterprises; declared division of roles with who installs and integrates, already written in `07_integration/09`; and the technical file with risk management, which is the **substantive** defense because the responsibility from product has as a defense the state of the art. **Not achievable, and must not be allowed to believe otherwise**: the manufacturer role is defined in **functional** way by art. 2, point 30, and does not transfer by contract; the responsibility toward the damaged patient **cannot be excluded by contract**, and the license clauses apply between the parties and not toward them; criminal responsibility is always personal; the administrator is still liable for gross negligence and violation of their own duties. **The open license does not protect**: who places on the market is a manufacturer even if the code is Apache-2.0. **Note of verification, corrected the same day.** The directive is **(EU) 2024/2853**, which abrogates 85/374/CEE, with transposition deadline **9 December 2026** and application to products placed on the market or put in service **after** that date. It had been marked `[NV]` for caution, but the project **had already ascertained it**: [`docs/10_fondamenti/20-fonti-primarie.md`](../10_fondamenti/20-fonti-primarie.md) §6.4 reports it with the three provisions used, art. 10, art. 11 and art. 15. The caution was unfounded and the `[NV]` is removed: see `Q-293`. A judgment of a lawyer in medical devices and a regulatory consultant is needed: what is registered here is the position, not replacing it

### Q-292

**From**: → **ORCH** · **To**: `COMP`, `ROAD` · **State**: OPEN

**Does the sequence of reception of `D63` precede or follow the already scheduled milestones?** The sequence goes through seven areas in five steps, and **each step is indivisible**: the criterion of `Q-200` is that partial rewriting produces two coexisting attributions, which is worse than an outdated attribution. Capacity is that of `D62`, ten-twenty hours weekly. The tension between indivisibility and capacity **is not resolved by formulating better**: it is resolved by deciding the order. Two facts to keep in mind while deciding. **The first**: in the waiting `OUT-20` of `03_functional/07-fuori-perimetro.md` remains **published and false in three clauses out of four** - constituting a subject, engaging bodies, conducting clinical evaluation are no longer "outside perimeter" - and that chapter is what a reviewer opens to learn what the project does not answer for. **The second**: `Q-289` has ascertained that the admitted conformity formulas are a rule written **without any automatic control**, and `D63` increases its exposed surface, so the sequence is executed without net. Decision of the client

### Q-293

**From**: → **ORCH** · **To**: `COMP` · **State**: RESOLVED

**RESOLVED the same day it was opened, and the lesson is worth more than the issue.** It was opened as "conflict on a regulatory source": `Q-291` marked `[NV]` number, date and transposition deadline of the directive on liability for product defects, while documents cited it in full. **It was not a conflict: the `[NV]` was unfounded.** [`docs/10_fondamenti/20-fonti-primarie.md`](../10_fondamenti/20-fonti-primarie.md) §6.4 carries the citation already verified - **Directive (EU) 2024/2853**, transposition deadline **9 December 2026**, applicable to products placed on the market or put in service **after** that date, with the provisions used identified one by one: art. 10 on presumption of defect, art. 11 on the exemption of the manufacturer of a component, art. 15 on the prohibition of excluding by contract the liability toward the injured. **Eleven places cite it, not three.** The error is the orchestrator's, who declared uncertain what the project had already ascertained without seeking the document of primary sources. It is the specular defect of `A-1` of the runbook of errors: there one had deduced a convention without reading the rule, here an uncertainty was declared without reading the corpus. **In both cases the cause is the same: not having opened the document that exists for that purpose.**

### Q-294

**From**: → **committente** · **To**: `SEC` · **State**: **OPEN**

**Ratification of the 24-hour commitment to integrators.** `SECURITY.md` now declares, in both languages, that the project notifies who integrates of an **actively exploited** vulnerability within 24 hours of acquisition of the evidence, immediately if exploitation is in progress on operational installations, and **regardless of availability of the fix**. The deadline was missing, and without it the integrator cannot meet their own 24 hours: it was the gap `RS-16`. But it is a **statement of public policy**, proposed by `SEC` and not yet approved: either the client ratifies it, or it must be withdrawn from the public file. Stated reference: notification obligations of art. 14 of Regulation (EU) 2024/2847, applicable from **11 September 2026** (art. 71), citation already ascertained in [`docs/10_fondamenti/20-fonti-primarie.md`](../10_fondamenti/20-fonti-primarie.md) §6.3

### Q-295

**From**: → **`TECH`** · **To**: `SEC`, `TECH` · **State**: **OPEN**

**A `non_databile` status for the controls table**, twin of the `non_collaudabile` proposed for `Q-288`. Rule 2 of `pipeline/README.md` does not admit a row in sole flagging **without date**, and for `SEC-C8` - conformity of identity federation - no dated milestone carries its object, because it requires environments and credentials not under the project's control. The row thus lives as a **comment block** at the end of the table, with identifier, reason and the two sources that ascertain it, and the convention is codified in `pipeline/README.md`. It is an honest solution to a structural conflict, not a solution

### Q-296

**From**: → **`TECH`** · **To**: `SEC`, `TECH` · **State**: **OPEN**

**Placement of the nine `SEC-Cn` in a construction flow, and stable assignment of the designation.** The nine rows added have `eseguibile = da collocare`: until the work exists in the flow, the declared date is **a deadline without an executor**. The first to expire is `SEC-C2`, analysis of dependencies against vulnerability databases, the **26 September 2026**, and it is the only one that already has an object because the bill exists. It must also be decided whether `SEC-Cn` is the stable designation or whether the table `G1`…`G13` of `docs/01_technical/09` §3, today saturated and without any of the ten activities, should be extended

### Q-297

**From**: → **`ARCH`, `COMP`** · **To**: `ARCH`, `COMP` · **State**: **OPEN**

**The architectural area has never received the regulatory framework, and `D63` has not crossed it.** Verified on the text: searching for marking, MDR, IEC 62304 and manufacturer in `docs/adr/` and `docs/02_architecture/`, the only occurrences are `ADR-0031` and the **temporal** marking of `ADR-0013`. There are no phrases made false by `D63`: there is an area that has never posed the question. The hole does not close by adding citations to the thirty existing registers, but with **a new register** that declares the architectural consequences of the manufacturer role: (a) traceability as a property of module boundaries and not of documentation alone, which if decided after criterion 1 of `T-08` forces redoing the modules; (b) **segregation between elements of different class**, which `docs/08_compliance/03` §325-327 requires and which no one has designed; (c) the **re-reading criterion** of the thirty registers decided under the premise that the project was not a manufacturer - without that criterion the re-reading will not happen, or will happen thirty times in different ways. It must be written by `ARCH` **with** `COMP`, not by one without the other

### Q-298

**From**: → **`ARCH`** · **To**: `ARCH` · **State**: **OPEN**

**`D61` is a decision on bounded contexts without register.** Verified: `docs/09_roadmap/00-indice.md:172` registers it, `.telemedic/context/00_PROJECT_BRIEF.md:313` carries the motivation, and **no file of `docs/02_architecture/` or `docs/adr/` names it**; `ADR-0023` treats the thirteen contexts as received data. The divergence falls on the **tree of sources** - three documents list twelve placing the terminology gateway under `platform/`, two list thirteen - i.e., on what criterion 1 of `T-08` crystallizes on 14 November 2026. The consequence missing today: if the gateway is a bounded context it has its own schema per tenant, so **its own outbox**, something a cross-cutting component would not have had. The register was not written on purpose: a decision register is an act, not a compliance

### Q-299

**From**: → **`ARCH`** · **To**: `ARCH` · **State**: **OPEN**

**`ADR-0010` still declares the order by key without conditions** in its own Decision row, while the chapters have been made conditional. It was not amended on purpose: **modifying the decision row of an accepted register is an act of decision**, not an editorial correction, and must be done by substituting or amending the register. The concrete risk: who applied the rule "the register prevails on the chapter" would annul the correction just made

### Q-300

**From**: → **`TECH`** · **To**: `ORCH` · **State**: **OPEN**

**The missing control is on the form of tenant identifier, not on the equality of code blocks.** Tonight a defect passed between the languages: two event envelopes corrected in Italian and left in English with a tenant name speaking, and the divergence control has continued to declare "no divergence" because it compares the structure of files, not what is inside a fenced block. **The first proposal - "code blocks must coincide between the two languages" - has been measured and rejected**: on 145 pairs of files, **78 have at least one divergent block**, and the divergence is legitimate, because blocks contain diagrams with **translated labels**, which is prose. A control like this would fail everywhere and be turned off, which is worse than its absence. The right control is **narrow and already declared**: `EV-13` in the table of mandatory verifications of [`docs/02_architecture/06-eventi-e-integrazione-interna.md`](../02_architecture/06-eventi-e-integrazione-interna.md) §9 - "no published envelope contains the **name** of a tenant" - to be realized as a control that searches for the forbidden form **inside the envelopes, in both languages**, independent of translation. It is the same schema of `verifica-dati-sintetici.sh`: one searches for a forbidden form, not comparing two texts

